# PR31 修复验证记录

状态为 `in_progress / outcome_pending`。未合并、未激活发布，未签用户视觉复核或最终文件批准。此页记录已经发生的验证，不将历史证据重新标为最终候选结果。

## 已执行的工程与工具验证

- 完整修订快照、父修订身份、锁内输入核验、同内容重放、取消和预算、并发及真实进程终止/重启恢复已有对应反例测试。正式消费者从捕获的修订读取。
- 路由覆盖真实 CLI、新旧运行边界、创建模式不可覆盖；独立复审新增“先有内容快照、后首次路由”缺口已在 `7381e04` 修复，原反例复核通过。
- 新公共内容可通过正式 `page-content prepare/submit` 接续，真实 ImageGen 回传以及 SVG/Scene 配对提交已在十页冷启动运行执行。图片多余标签和单页正文不足均保留失败与局部返修记录。
- LibreOffice、pdftoppm、Noto Sans SC 真实编译/渲染/回读已执行。候选 `4a808c5241c04771de20de9a1d47b41dca3a1dfa` 的隔离发布安装、源码与旧后端访问拒绝、两页安装后渲染、七类编译保真及迁移回滚全部通过；此证据不代表之后候选已复验。
- 七类编译保真保持既定 SSIM 与几何阈值。ImageGen→SVG 七类页已产生逐页辅助评分与对照；辅助评分不代用户复核。
- LibreOffice 实际修改中文文字、原生组、箭头和颜色；保存文件经重新载入、渲染及对象差异核验。录像只证明所捕获片段，不能单凭录像声称全过程完整。原 PPTX 哈希保持。
- 托管 PPT Library 实际索引与查询已执行；角色标签不全如实报告降级，合法空结果与程序故障分开验证。
- 独立复审另发现外部审查任务/归档 symlink 输出和否定裁决子串问题，分别在 `ca08a1d`、`6442868` 与 `12aa5ec` 修复并交叉复核。
- 真实独立图片编译暴露回读图片计数遗漏，`70844a4` 已修复；缺字体的真实编译和就绪状态曾误报通过，新增反例保留原产物哈希并要求阻断。研究无工具或无结果终态曾在续跑时消失，现保留未解决问题、受影响引用及宿主处理任务，禁止自动重派消耗预算。

## 全量回归与候选边界

`4a808c5` 在隔离 Python 3.11/3.12 中各完成 1847 passed、6 failed、121 subtests passed。三项失败为旧契约断言，三项为隔离 HOME 中缺少 Playwright 浏览器缓存；断言已修复并保留拒绝旧审查、内容变化失效和模式不可覆盖的验证。不得把这一轮描述为全绿。

`d49a775` 启动后续全量回归及独立发布验收。远端 CI 首轮另定位到测试辅助模块依赖手工 PYTHONPATH 的收集错误，已补测试自身导入路径。最终结果应以同一候选的实际日志和 CI 为准；此文不预填结果。

`b165d45d5a992ef9ea2a5555ed8d5b3b42019a05` 在隔离 Python 3.11/3.12 中各完成 **1872 passed、121 subtests passed**；同一 SHA 的独立发布安装、源码访问拒绝、真实编译渲染与迁移回滚亦通过。随后新增修复需在最终候选重新验证，不能沿用该 SHA 的通过结论。

## 当前候选检查点

- `0cfbe54` 两个 Python 全量各 1880 passed；远端 CI 因字体与 3.11 环境内的真实 3.12 安装依赖缺失失败。后续固定字体来源和哈希，CI 显式安装实际 3.12 运行依赖，未将旧 CI 失败改记通过。
- `09b7587` 两个 Python 各 1946 passed、2 failed。失败分别为圆角面板可编辑对象增加导致旧计数断言失效，以及旧 fixture 必然低视觉分的假设；已改为精确对象与受控内容错误断言。
- `848134b` 隔离发布树安装、源码与旧后端访问拒绝、旧运行迁移/回滚、真实两页渲染和七类编译保真全部通过。两个 Python 全量各 1979 passed、2 failed、121 subtests passed；两项隐藏 tspan 测试实际正确阻断，仅错误消息匹配过时，`75270a7` 更新精确异常/页面断言，未改安全逻辑。
- `848134b` 冷启动十页重新编译后，21 项输入、逐页 PPTX 文本与实际 PNG 经独立审查，三项正式审查导入通过；仍未取得用户批准。之后向同一运行补入实际公开案例适用性研究，最终内容指纹与审查需重新核验。
- 真实已安装入口暴露 native 提示仍引用旧 PPT Master、缺工具恢复被可选 Library 遮挡。`c036d7a` 修正提示与恢复优先级，两 Python 各 29 项通过；最终安装回放仍须执行。
- 相同批准 Package/Lock 分别通过 image_blueprint 和 direct_svg 生成两份真实两页 PPTX，全部文字、数字和限定一致。direct 使用正式 SVG/Scene 提交；图片模式保留真实 ImageGen 来源和原预算，未伪装新的图片调用。
- 真实混合 PDF/DOCX/XLSX/PPTX/图片/文本输入完成提取、缺页阻断和完整输入恢复；长文本尾部限制保留。安装依赖全部版本与官方 wheel 哈希锁定，独立重复安装对比通过。
- 当前逐项工程谓词记录为 141 verified_pass、5 partial、1 human_pending；这不是最终同 SHA 验收。完整 147 项及原始证据哈希见 [脱敏证据索引](pr31-evidence-index.json)。新增补证继续更新，原始来源 SHA 不回填为新候选。

## 最新可复核检查点

- `7a8f753` 两个 Python 全量各 **1989 passed、121 subtests passed**，3.12 覆盖率 **89%**；隔离发布树真实生产通过。
- `8c521a5` 修复研究摘要升级完整宿主捕获的正式入口；同一来源 URL 和旧摘要哈希锁定，保留旧证据、研究预算和客户未知事实。独立正反例及真实安装入口已执行。
- `4e23e57` 隔离发布安装、真实编译渲染、迁移回滚与七类编译保真通过；两个 Python 全量各 **1997 passed、121 subtests passed**，3.12 覆盖率 **89%**。冷启动十页重新构建并独立导入三类审查，技术就绪，不代表用户批准。原十页独立复核仍为 P008/P006 返修。
- `73399fc` 补齐正式 `workflow questions/answer`，回答经过输入令牌、当前问题、来源权限及修订事务；真实 CLI 合成否定分支通过，53 项目标回归通过。
- `de2dec6` 修复旧 canonical 审查输入变化后仍阻断的问题。旧报告保留为 stale；当前有效 P0/P1 继续阻断，重新派发同输入任务不能清除问题。两 Python 各 24 项及 42 项消费者回归通过。
- 独立实际 CLI 反例发现首次修订前的问题依赖没有进入输入令牌，`f10a7d5` 已修复，38 项通过。正式导入反例还发现同输入替换报告会隐藏 P1，`bf779ac` 拒绝移除/降级当前阻断，并使实际消费者读取仍有效的归档问题；两个 Python 各 85 项通过。
- 上述修复仍需最终候选交叉复审、全量与发布验证；不把历史 SHA 证据改标最终通过。

## 尚未收口

1. 原十页 P008 的阶段退出条件保留 P1；P006 指标口径和文字布局保留两个 P2。两页均有具体返修稿，重建预算均已达三次；各增加一次额度须用户明确决定。保留失败记录，不改 task_id、清账或新建运行绕过。P004 已局部修复，保留其余九页输入哈希。
2. 跨页决定保留、安装后实际方法入口与缺工具恢复、代表页检查和局部续跑已补实际证据；五项 partial 涉及方案差异人工复核、七类视觉达标和修订前后最终批准。P006 当前辅助评分 2.6、七类均分 3.8857，均未达到门槛，不能归为仅等待人工确认。
3. 最终候选全量回归、覆盖率、CI、发布树和独立复审须全部完成；所有工具证据保留实际版本和限制，不推断其他桌面平台兼容。
4. 集中复核包含两套十页三列对照、七类辅助评分、桌面编辑证明与最终文件哈希。人工视觉复核和文件批准待用户完成，不代填。

原始图片、录像、日志和运行快照保留本地验收包；仓库仅保留公开合成样例、代码、测试和脱敏状态。客户效果配对属于 L3，不纳入本轮，也不标 `accepted`。
