# T12｜局部修改与并发恢复

状态：**页内编辑与失效传播已实现；并发恢复矩阵待补**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

读集重基、稳定页ID/增删重排、跨页事实、取消竞态、晚到保护、恢复与重定位；先单页编辑/无关页不重生，再补全并发与恢复；恢复内容不重置调用记录；样式/资产按实际依赖失效。

- 所属工作包：[WP03](../work-packages/WP03.md)。
- 最早启动：T02.min,T10.min。
- 整项完成依赖：T02,T10,T11。
- 早期交付：T12.min：一次单页修改、旧原件保留/无关页不重生；先实际阅图再补统一自动关联。
- 必读规范：[03-data-and-state-contracts.md](../specs/03-data-and-state-contracts.md)、[08-edits-recovery-and-budget.md](../specs/08-edits-recovery-and-budget.md)、[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。
- 输入样例：[完整往返例](../examples/roundtrips/README.md)及[五份拟实施契约](../contracts/)。示例只用于协议对照，不是实际通过记录。

## 修改范围与接口

request_edit、commit_change、history restore；read_set 按页及真实共享输入；全稿顺序/恢复要求明确 base_revision。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/store.py`
- `src/deck_master/tasks.py`
- `src/deck_master/service.py`
- `src/deck_master/production.py`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T12.01 先交单页修改T12.min

- [ ] 实施：request_edit带scope和read_set；正文/边修改产新Page/SVG/PPT并刷新文件检查；分别验证轻微文本修改和需重生蓝图的重大目标页修改。
- 交付与核对：目标p09按实际影响修改，必要时重生该页蓝图；其他未受影响页蓝图/SVG hash不变、无新增外部制作；旧件可找回。

### T12.02 实现影响范围与页集合

- [ ] 实施：按正文/有效样式/字体/资产/共用事实依赖传播；Document负责增删重排。
- 交付与核对：删页不由glob混回；重排仅更新实际页码/引用与装配，未用样式不重生。

### T12.03 实现幂等与并发重基

- [ ] 实施：相同operation同结果复用，异结果冲突；不同页可重基，同页和共享输入变化拒绝。
- 交付与核对：两写者并发反例无最后写覆盖；超时重交不生重复版本。

### T12.04 实现取消和晚到结果保护

- [ ] 实施：start/cancel/accept同项目事务；本地子进程仅管理所属进程；晚到可结算调用但不采用内容。
- 交付与核对：cancel先赢不写，accept先赢不删产物；取消后的同hash结果也不接受。

### T12.05 实现历史恢复和目录搬迁

- [ ] 实施：restore生成新revision并保留操作事实；object相对Ref；源重映射核验hash、字体重新定位。
- 交付与核对：复制项目后产物可看，调用消耗/未知/停止不回滚，缺源只影响需原文动作。

### T12.06 验证跨页与设计修改

- [ ] 实施：共用事实、默认style、独立style、同asset换字节、画布变化成组回归。
- 交付与核对：所有相关页更新；不相关页保留；完整卡由T11复核当前状态后关闭。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-S03 Task幂等 | 同operation相同输出重复返回同结果；不同输出拒绝 | `tests/rebuild/test_tasks.py` | 本卡关闭 |
| AC-S04 依赖分层 | 预算/备注不让未变SVG失效；实际字体/源事实/全局style改变影响正确范围 | `tests/rebuild/test_store_transactions.py` | 本卡关闭 |
| AC-S05 局部编辑 | p09修改后无关页原图/SVG hash不变；整PPT允许重装配 | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-S06 多页事实 | 改变共用事实/关系，所有引用处更新，不只修改点选页 | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-S07 页集合 | 增删重排稳定ID，删页历史保留不混回当前PPT | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-S08 并发与冲突 | 不同页读集安全重基；同页冲突不最后写覆盖 | `tests/rebuild/test_store_transactions.py` | 本卡关闭 |
| AC-S09 取消竞态 | cancel先赢则晚到不写；accept先赢则cancel不删除成品 | `tests/rebuild/test_tasks.py` | 本卡关闭 |
| AC-S10 恢复/重定位 | 恢复新revision不改旧快照；移动项目refs可读，源映射检查hash；design_context相对对象/字体实际指纹恢复；不恢复已消费调用额度 | `tests/rebuild/test_store_transactions.py` | 本卡关闭 |
| AC-K15 资产字体重定位 | 移动项目后Logo/icon从对象Ref读取；系统字体实际hash变化报告并重检，缺字体不阻旧媒体查看；不打包字体到发行 | `tests/rebuild/test_sources.py` | 本卡关闭 |
| AC-S13 只改有效样式 | 默认style改动仅影响依赖它的页；独立覆盖页与未用style/asset不重生；同asset_id换字节使相关页失效 | `tests/rebuild/test_service_flow.py` | 本卡关闭 |

阶段命令、测试用例责任和正反例见[阶段交接与验证](handoffs.md)。先运行本阶段用例，完整模块仅在整卡范围已实现后运行；缺测试不得用skip代替。

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：发现无关重生则修依赖；不清整目录或删除旧产物。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：待填
已完成子任务 / min或整项：待填
实际命令与结果：待填
AC证据与未验项：待填
下游接收接口 / 文件 / 版本：待填
阻碍与下一动作：待填
```
