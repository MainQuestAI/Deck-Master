# T06｜完整prompt与真实工具任务

状态：**本卡实现与验收完成，整卡关闭待依赖**（2026-09-16）。T04 仍为 min，AC-C02/C07 真实内容证据未关闭；本轮不进入 T07。

## 目标与输入

全字段投影；Host真实图像输入/输出；缺工具待执行，不降级；实际prompt来源如实；使用Document解析的画布/字体/style_ref/允许资产；首次真实调用前接T13.min额度。

- 所属工作包：[WP02](../work-packages/WP02.md)。
- 最早启动：T03.min,T04.min,T13.min。
- 整项完成依赖：T03,T04。
- 早期交付：T06.min：当前事故页完整prompt与允许资产真实调用。
- 必读规范：[03-data-and-state-contracts.md](../specs/03-data-and-state-contracts.md)、[05-blueprint-and-svg.md](../specs/05-blueprint-and-svg.md)、[08-edits-recovery-and-budget.md](../specs/08-edits-recovery-and-budget.md)、[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。
- 输入样例：[完整往返例](../examples/roundtrips/README.md)及[五份拟实施契约](../contracts/)。示例只用于协议对照，不是实际通过记录。

## 修改范围与接口

production.project_prompt(page, resolved_design_context, permitted_assets)；blueprint.result 文件/来源记录；调用必须经过 begin/settle。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/production.py`
- `skills/deck-master/references/blueprint-svg.md`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T06.01 实现完整prompt单次投影

- [x] 实施：production.project_prompt同次生成prompt文本和字段映射；消费完整Page与已解析设计配置。
- 交付与核对：marker落在真实prompt，覆盖副标题/所有嵌套正文/数值单位/脚注/节点边/允许资产。

### T06.02 连接真实blueprint任务

- [x] 实施：派发实际可读Page、prompt、画布和资产；Codex调用真实图像工具，缺工具明示。
- 交付与核对：不同页按真实内容设计；不以hash差异证明实际独立生成。

### T06.03 接入首次调用额度边界

- [x] 实施：依赖T13.min，在调用前start/begin、之后settle；无用户限制不设三次默认。
- 交付与核对：已发失败、未发和未知分开；本地产物接收失败不抹掉真实调用。

### T06.04 登记真实来源和版本

- [x] 实施：保存图片实际字节、可得invocation和真实submitted prompt；拿不到时标unknown。
- 交付与核对：记录可与真实工具调用对照，不用当前Page倒填原prompt。

### T06.05 验证设计要求实际生效

- [x] 实施：无品牌路径、指定画布/字体、同页Logo/VI对照与第二页差异化生成均已验证。
- 交付与核对：无品牌库可生产，给定VI进入真实prompt和实际阅图；T06.min交出当前页原图。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-B01 真实prompt全字段 | marker确认subtitle/footnotes/labels/visual_spec等进入实际prompt而非仅中间JSON；同Document有效design_context/style_ref/允许资产投影，不独立补默认 | `tests/rebuild/test_production.py` | 本卡关闭 |
| AC-B02 来源诚实 | actual prompt只从真实提交记录取得；未知调用标unknown；文件名/签名不升级真实性 | `tests/rebuild/test_production.py` | 本卡关闭 |
| AC-B09 无假生图降级 | 缺Host工具返回待工具，不走fixture/无字通用图/自动直绘替代任务 | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-R08 品牌按任务 | 没有品牌库不阻断；已给VI要求实际进入设计和检查 | `tests/rebuild/test_production.py` | 本卡关闭 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_production.py tests/rebuild/test_service_flow.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [x] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [x] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 整卡依赖关闭：T04 完整验收尚未完成；保留 T06.min 与本卡修复证据，不宣布整卡关闭。

失败返回：投影漏项直接修投影，不让后续SVG补假内容。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

- 源码基线：`1c2bb80`；本轮修复为当前未提交工作树，没有“整卡补齐提交”。外部证据索引记录关键源码逐文件 SHA256。
- 代码：项目全任务额度统计；写锁内原子派发和预留；旧空额度任务恢复；领取者/停止/unknown 校验；CLI 和结果信封共用结算校验；幂等、冲突和重复 invocation 保护。
- 设计：项目/页面许可集合与 external_use 一起校验；prompt 和附件一致；页面语言/字体覆盖生效；画布不再固定 16:9。
- CLI：执行型 continue 待 Host/工具/输入返回 3，JSON 在 stdout；存储版本冲突返回 5。
- 验证：`/tmp/deck-rebuild-dev/bin/python -m pytest -q tests/rebuild` → **105 passed**；含真实双进程争抢、指针故障、旧任务修复及结果信封反例。`git diff --check` 通过。
- 核心回归：`tests/rebuild/test_t06_boundaries.py`；保留并更新已有调用/CLI 测试的错误旧预期。

### 本轮真实调用

证据根：`/tmp/deck-master-t06-repair-20260916-v2`；`evidence-index.json` 包含源码身份、报告、prompt/image/Artifact hash 与验证结果。

| 条件 | invocation | 实际结果 |
| --- | --- | --- |
| 无品牌同正文 p09 | `exec-5410bdb5-d813-4bca-bc50-8ed06f38b218` | start/begin/settle/accept 完成；实际 prompt 与程序投影逐字相同 |
| NOVA 品牌同正文 p09 | `exec-e52375f7-04c1-4b47-a79d-f55aa61aef00` | 同上；附件取自本项目许可资产的不可变 PNG 字节 |

- AC-B01：完整展示字段、有效设计配置进入程序 prompt；两次实际提交均与投影一致，采用后重新读回验证。请求 1200×900，工具返回 1448×1086；4:3 生效，不能声称像素尺寸精确一致。
- AC-B02：原图、实际 prompt、invocation、consumed 记录齐全；证据等级为 host_reported，费用 not_reported，不声称 provider_verified。
- AC-B09：没有宿主执行时任务保持 awaiting_host；continue 返回 3；方法明示无工具不得降级。本轮当前宿主具有 ImageGen，没有声称在另一真实无工具宿主做过实测。
- AC-R08：无品牌可生成，NOVA Logo 与暖象牙/深青/珊瑚 VI 在品牌图可见；restricted/unspecified 与页面许可边界已有反例覆盖。Logo 可识别，不声称像素保真；字体要求进入 prompt，不声称工具精确使用指定字体。
- 阅图限制：两图总部专家名称位于正文而非独立节点标题；品牌图“本期只读接口”标签误放总部专家卡片，已登记 Artifact 限制，需后续内容审阅纠正。蓝图候选采用不代表业务验收通过。
- 既有无品牌/品牌对照与 p10 差异化路线图证据继续保留在 `/tmp/deck-master-t06-min-20260916` 和 `/tmp/deck-master-t06-full-20260916/project-v3`；本轮不重写旧调用记录。

### 依赖与下一动作

T03 交接回填报告整项交付，但卡头仍为旧状态，本轮未重新执行其全部资料格式验收。T04 明确只交付 min，AC-C02/C07 仍未验证。因此本卡自身修复与验收结束，**整卡关闭仍待 T04 依赖证据**；不进入 T07，不把本轮 105 项测试或两张图升级为整稿交付证明。历史恢复、完整重试仍由 T13 负责。

### 用户要求继续后的单页交付（2026-09-16）

用户随后明确要求补齐 SVG、单页 PPT 与实际渲染，覆盖前述“本轮不进入 T07”的执行范围限制。已完成 NOVA p09 单页下游 min：原图 → 原生 SVG → 原生 PPTX → LibreOffice 实际渲染与三方对照。整卡依赖状态不变。

- 仓库外交付：`/Users/dingcheng/Downloads/Deck-Master-single-page-20260916/output/`。
- 原生 PPTX：76 个形状，31 个文字段，0 图片对象，4:3；逐段回读与 SVG 一致。Logo/VI、数值、方向、图标已主审，标签归属已修正。
- 测试：`tests/rebuild` **111 passed**；新增 6 个 compiler 最小子集/失败不发布测试。源码为 `1c2bb80` 加工作树。
- 此次下游实现仅是当前页面适用子集，不关闭 T07–T10 全卡；T04 依赖仍未关闭。
