# 制作输入交接

负责人是 T01.05，消费者是 T06/T07/T08/T09/T10/T12。准备发生在开发阶段；本文件不表示已有样本通过验收。

| 样本 ID | 必须覆盖的内容 | 下游用途 |
| --- | --- | --- |
| REBUILD-ARCH | 三层职责、嵌套模块、图标、建设状态、双向/单向关系及脚注 | 第一张复杂页，两段还原、删除模块与反转箭头反例 |
| REBUILD-CHART | 数值、单位、图例、标签及比较关系 | 第二张图表页，数据语义及可编辑文字/形状 |
| REBUILD-SOLUTION | 方案理由、阶段/责任、辅助说明及明显视觉层级 | 第三张方案页，信息完整性和布局变化 |

在仓库外的 `$DECK_REBUILD_EVIDENCE/inputs/index.json` 建立本机映射，每项记录：sample_id、来源（原事故/公开/合成）、材料文件、Page文件及版本/hash、原始蓝图文件及hash、独立视觉期待文件、准备者、交接日期。消费者先验证路径可读和hash，不能只看到ID便继续。文件名和本机根目录由执行者选择并记录，私有路径及原件不提交仓库。

期待文件必须从原图与完整Page逐区域列出模块、图标、归属、状态、关系、必要数值单位和文字层级，记录对应原图区域；不能从缩减后的SVG或编译结果反推。T07实际读图后修订期待须保留原因和原版本。

原事故图可取得时登记真实原件；不可取得时，由 T01.05 使用明确标注的合成Page与同等复杂度设计需求准备替代输入，T06真实生图，T07阅图后形成原图基准。因此 T01.05 初次交接可只含Page/设计需求及 blueprint_pending，不能阻断T06；T08几何开发可用已有SVG样本，T10还原验收必须取得T07确认的真实蓝图。原事故未复跑仍标未复跑，不能以替代样本关闭事故复现结论。

T10.03 使用 REBUILD-ARCH；T10.04 扩展另两页。每次保留原图、SVG、PPT、两段渲染及实际观察，T12从其副本开展轻微文字和重大图示修改。
