# T16｜安装器与诊断提取

状态：**整卡完成**。负责人：Codex + Kimi Code 收尾。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

隔离release/固定解释器/原子激活回滚；资源完整、按步骤报告依赖；旧Skillownership清理。

- 所属工作包：[WP04](../work-packages/WP04.md)。
- 最早启动：T05,T10。
- 整项完成依赖：T05,T10。
- 早期交付：按整项任务依赖执行。
- 必读规范：[01-target-architecture.md](../specs/01-target-architecture.md)、[11-package-install-and-cutover.md](../specs/11-package-install-and-cutover.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

release/current/previous；release.json 固定安装版本；doctor --step 只报告本步真实依赖。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/install.py`
- `src/deck_master/doctor.py`
- `tools/build_release.py`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T16.01 提取独立发行构建

- [x] 实施：tools/build_release调用正常PEP517资源hook；release记录精确源码/包/方法/内核版本。
- 交付与核对：wheel/sdist资源完整，新包不引旧命名空间。

### T16.02 实现固定解释器安装

- [x] 实施：新release目录独立venv、实际模块路径与按步探针；不改系统Python。
- 交付与核对：隔离前缀安装可导入/读取资源/调用编译；不借旧安装补依赖。

### T16.03 实现原子激活与回滚

- [x] 实施：检查通过后在测试前缀切current/previous；失败不动旧current；旧代码不写新格式。
- 交付与核对：故障安装原链接不变；rollback回旧二进制并提示格式边界。

### T16.04 实现按步doctor

- [x] 实施：compose/blueprint/compile/render/view/export分别报告解释器/工具/字体/来源。
- 交付与核对：缺renderer不阻正文，无PPTMaster/Library不报新核心缺绑定。

### T16.05 实现旧Skill所有权处理

- [x] 实施：仅替换安装器拥有且未被改动的资源；第三方/用户同名内容保留。
- 交付与核对：三种归属反例真实验证，激活用户当前版本留到授权实施阶段。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-I01 包依赖边界 | 新src不import旧命名空间；compiler不反向依赖业务审批 | `tests/rebuild/test_package_boundary.py` | 本卡关闭 |
| AC-I02 实际doctor | 解释器、模块路径、字体、renderer按步报；不把安装就绪当内容专业 | `tests/rebuild/test_install.py` | 本卡关闭 |
| AC-I03 安装回滚 | release先测后原子激活；失败保留current，回滚不让旧代码写未知新格式 | `tests/rebuild/test_install.py` | 本卡关闭 |
| AC-I07 Skill不误删 | 只替换安装器拥有且未改的旧资源；同名第三方Skill不动 | `tests/rebuild/test_install.py` | 本卡关闭 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_install.py tests/rebuild/test_package_boundary.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [x] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [x] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [x] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：安装失败不改current；不借源码目录补文件。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：Kimi Code 收尾（install.py/doctor.py 切片为前序提交）
已完成子任务 / min或整项：整卡完成（安装器 candidate/activate/rollback、按步 doctor、隔离候选先验）
实际命令与结果：.venv/bin/python -m pytest -q tests/rebuild/test_install.py → 14 passed（含 test_candidate_install_activate_and_run_doctor：build_release 真实 manifest → install_candidate → activate → 激活 venv doctor --step view ready 且 module_path 指向前缀内、不借源码树；既有 activate/rollback 失败保留测试同绿）；doctor 分步输出与 README Diagnostics 口径一致（compose/render/export 逐步,缺工具 needs_tool 带真实原因）
AC证据与未验项：AC-I05/I06 的 T20 隔离安装端到端证据在 test_install.py（wheel+sdist 隔离 venv、空 HOME、模块来源/包 SHA/资源路径记录）；T16 本卡交付安装器与诊断闭环。未验项：无
下游接收接口 / 文件 / 版本：deck_master.install.install_candidate/activate/rollback；deck_master.doctor.diagnose(--step compose|blueprint|compile|render|view|export)
阻碍与下一动作：无
```
