# T08｜纯SVG接口与派生IR

状态：**整卡完成（2026-09-20）；通用声明子集正反例测试补齐，AC-K09/AC-B06 有关闭证据**。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

对比B0/K0提取SVG解析，建立受控子集和纯接口；Host单写SVG；禁止反向import旧engine；首版形状文字可编辑，图表/表格非Office原生数据对象；不要求Host补chart数据接口。

- 所属工作包：[WP02](../work-packages/WP02.md)。
- 最早启动：T01边界,T02.min。
- 整项完成依赖：T01,T02。
- 早期交付：T08.min：独立SVG接口与当前事故页实际子集。
- 必读规范：[01-target-architecture.md](../specs/01-target-architecture.md)、[05-blueprint-and-svg.md](../specs/05-blueprint-and-svg.md)、[06-native-compiler.md](../specs/06-native-compiler.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

compile_deck(SvgInput[], CompileOptions, output_dir) -> CompileResult；IR 只从 SVG 派生。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/compiler/__init__.py`
- `src/deck_master/compiler/api.py`
- `src/deck_master/compiler/ir.py`
- `src/deck_master/compiler/svg.py`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T08.01 比较并提取独立接口

- [x] 实施：逐项比较B0/K0转换实现与依赖；建立SvgInput/CompileOptions/CompileResult。
- 交付与核对：只传SVG/资产/参数可调用，compiler不读取HOME/旧OS/审批。

### T08.02 实现SVG到派生IR

- [x] 实施：解析受控元素、嵌套变换和属性；保留text/node/edge绑定与element定位。
- 交付与核对：Host只交SVG；IR由同份SVG派生，无第二份手写Scene。

### T08.03 实现自包含资源和明确诊断

- [x] 实施：本地symbol/use与批准图片解析；不支持特性给page/element/feature/recovery。
- 交付与核对：脚本、事件、实体扩展与外部可执行引用拒绝；正常不支持图形不静默删除。

### T08.04 固定配置与编译输出接口

- [x] 实施：canvas/font/asset由调用方传入；输入字节一致性检查；输出到新目录交store采用。
- 交付与核对：读取中输入变化不能登记成功，CompileResult只报告真实产物诊断。

### T08.05 交付事故页所需最小子集

- [x] 实施：按实际原图需要完成SVG解析与IR投影，将图标、连线、文字完整交给T09绘制；不在本卡重复实现原生绘制。
- 交付与核对：解析保留全部必要元素和定位，纯接口和错误诊断可测试；实际隔离PPT生成在T10关闭AC-K01。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-K09 不支持与安全 | 脚本/外部可执行引用拒绝；不支持特性定位对象，不静默删图 | `tests/rebuild/test_svg_parser.py` | 本卡关闭 |
| AC-B06 单手写源 | 只提交SVG可生成IR；Host无需手写Scene；必要绑定能解析 | `tests/rebuild/test_svg_parser.py` | 本卡关闭 |

阶段命令、测试用例责任和正反例见[阶段交接与验证](handoffs.md)。先运行本阶段用例，完整模块仅在整卡范围已实现后运行；缺测试不得用skip代替。

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：抽取牵出旧状态则切依赖，不整目录搬入。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：Kimi Code（工作树待提交：geometry.py、svg.py 修复 + 新建 tests/rebuild/test_svg_parser.py）
已完成子任务 / min或整项：T08.01–T08.05 全部完成，整卡关闭；min 切片证据见 2026-09-16 单页交付记录
实际命令与结果：.venv/bin/python -m pytest tests/rebuild/test_svg_parser.py -q → 43 passed；tests/rebuild 全量 180 passed（原137+新43）
AC证据与未验项：AC-K09 = test_unsupported_and_unsafe（36个参数化反例，脚本/事件/DOCTYPE/ENTITY/外链use/未批准image/滤镜蒙版裁切/未知元素属性/非法渐变与arc flags/非有限数值/越界opacity等，均断言SvgError且诊断含page/element定位与recovery，不静默删图）；AC-B06 = test_svg_to_ir（全子集正向解析，Host只交SVG即得IR，data-atom-id绑定进入IR，data-*其余属性放行）。未验项：AC-K01 实际隔离PPT生成归 T10 关闭
下游接收接口 / 文件 / 版本：deck_master.compiler.svg.parse_svg/SvgError（src/deck_master/compiler/svg.py）；path解析经 geometry._parse_path（修复arc flags严格校验0/1）；text非有限数值现显式SvgError；malformed style声明诊断可读
阻碍与下一动作：无；T08 关闭，T09 可领取
```

### 2026-09-20 整卡关闭记录

通用声明子集正反例测试补齐：新建 `tests/rebuild/test_svg_parser.py`（43 项），覆盖 spec 06.2 全部正向条目（基本几何含 path M/L/H/V/C/S/Q/T/A/Z 绝对相对、嵌套变换、linear/radial gradient、中文多行 tspan、symbol/use、批准 PNG 三种 preserveAspectRatio、直线端点方向、绑定解析）与显式不支持反例（均带 page/element/feature/recovery 定位）。修复 3 处实现 bug：geometry `_parse_path` arc flags 浮点截断（0.5 被当 0）、svg.py text 分支非有限数值漏检（nan/inf 静默入 IR）、style 声明缺冒号时诊断不可读。未削弱任何既有拒绝行为，未 skip。


### 2026-09-16 单页 min 交付

详见 [单页交付记录](../single-page-delivery-20260916.md)。本页已实际输出 SVG、可编辑 PPTX、渲染与回读证据；原始全卡子任务及 AC 未据此全部勾选。
