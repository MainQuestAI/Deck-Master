# T09｜绘制缺陷逐项修复

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

修端点/圆边界/800900字重/0透明度/多行与圆角；保留实际PPT XML反例。

- 所属工作包：[WP02](../work-packages/WP02.md)。
- 最早启动：T08.min。
- 整项完成依赖：T08。
- 早期交付：T09.min：当前事故页涉及的绘制缺陷先修；不简化原图绕过。
- 必读规范：[06-native-compiler.md](../specs/06-native-compiler.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

IR 保留原始端点、仿射变换、画笔零值和文字 run；DrawingML 写入与实际读回一致。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/compiler/geometry.py`
- `src/deck_master/compiler/paint.py`
- `src/deck_master/compiler/text.py`
- `src/deck_master/compiler/drawingml.py`
- `tests/rebuild/fixtures/compiler/`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T09.01 修方向和边界几何

- [ ] 实施：保留原line端点与方向；圆/椭圆变换求正确极值而非四点采样。
- 交付与核对：负斜率、反向线和旋转r10圆的实际XML/边界符合独立数值期待。

### T09.02 修画笔零值与组合

- [ ] 实施：fill/stroke/gradient stop/group opacity显式区分None与0；保留颜色和有效描边。
- 交付与核对：零透明度真实写入PPT，组合透明度和渐变反例可读回。

### T09.03 修字重字体与中文排版

- [ ] 实施：800/900不降normal；保留tspan基线/行距/run；记录实际字体匹配与声明fallback。
- 交付与核对：文字XML与SVG/PPT局部渲染同时对照，拼接全文正确不能抵扣布局错误。

### T09.04 保留圆角与缩放既有修复

- [ ] 实施：核对B0有效实现；等比和非等比变换下明确支持边界。
- 交付与核对：圆角/图标描边无回退；近似转换有对象级限制说明。

### T09.05 贯通DrawingML并交T09.min

- [ ] 实施：把事故页所需修复先接实际编译；其余声明特性随后补齐。
- 交付与核对：独立坏样例修前失败修后正确；事故页不用重画成更简单卡片。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-K02 负斜率与端点 | 正负斜率及反向line的实际PPT端点方向正确，不只bbox相同 | `tests/rebuild/test_geometry.py` | 本卡关闭 |
| AC-K03 旋转圆边界 | r10圆旋转45度仍20×20；椭圆变换使用正确极值 | `tests/rebuild/test_geometry.py` | 本卡关闭 |
| AC-K04 字体权重 | 800/900不反为normal；记录实际字体匹配/替代，XML与渲染核对 | `tests/rebuild/test_text.py` | 本卡关闭 |
| AC-K05 零透明度 | fill/stroke/gradient stop/group opacity=0保留；None才默认 | `tests/rebuild/test_paint.py` | 本卡关闭 |
| AC-K06 多行中文 | tspan位置/基线/行距/字重/换行保留；无精确字符串掩盖排版损失 | `tests/rebuild/test_text.py` | 本卡关闭 |
| AC-K07 圆角和描边 | 缩放圆角矩形、图标描边既有修复不回退；非等比范围明确 | `tests/rebuild/test_geometry.py` | 本卡关闭 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_geometry.py tests/rebuild/test_paint.py tests/rebuild/test_text.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：内核失败修对应函数，不把源图简化绕过。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：待填
已完成子任务 / min或整项：待填
实际命令与结果：待填
AC证据与未验项：待填
下游接收接口 / 文件 / 版本：待填
阻碍与下一动作：待填
```
