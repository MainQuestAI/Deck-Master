# T04｜宿主默认成稿和主入口

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

实现create/continue/task accept骨架；D1/D2成对方法写进实际Host输入；不再调用规则Planner和循环claim；固定临时结果信封与往返；首次正文后主Skill自动view --open；不需用户额外执行。

- 所属工作包：[WP01](../work-packages/WP01.md)。
- 最早启动：T02.min,T03.min。
- 整项完成依赖：T03。
- 早期交付：T04.min：create/accept/continue及D1/D2正常Host输入，信封与稳定ID。
- 必读规范：[01-target-architecture.md](../specs/01-target-architecture.md)、[04-host-content-method.md](../specs/04-host-content-method.md)、[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)、[14-agent-execution.md](../specs/14-agent-execution.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。
- 输入样例：[完整往返例](../examples/roundtrips/README.md)及[五份拟实施契约](../contracts/)。示例只用于协议对照，不是实际通过记录。

## 修改范围与接口

create/continue/task accept；kind 信封按 09.7 解析；一次接收预校验全部结果后原子采用，不新增永续结果对象。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/tasks.py`
- `src/deck_master/service.py`
- `src/deck_master/cli.py`
- `skills/deck-master/SKILL.md`
- `skills/deck-master/references/content-methods.md`
- `skills/deck-master/references/content-examples.md`
- `tests/rebuild/fixtures/content_pair/`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T04.01 实现创建与连续推进

- [ ] 实施：service/cli接create、--draft、import draft、continue；返回稳定待Host任务，复用已确认决定。
- 交付与核对：普通材料目录无库无模板可开始，重复continue不造重复任务。

### T04.02 实现五种结果接收

- [ ] 实施：按kind解析compose/blueprint/reconstruct/review/repair；核验scope、files及引用后一次采用；调用结算独立保留。
- 交付与核对：五种result-envelope走真实accept往返；坏路径/旧输入/同operation异结果拒绝且current不变。

### T04.03 接入成对内容方法

- [ ] 实施：在实际compose输入加载source-reading/content-methods/content-examples；同源D1/D2推导受众待判断问题和完整正文。
- 交付与核对：实际Host输入包含资料与方法；没有循环claim、关键词页角色或固定CTA补页。

### T04.04 贯通CLI状态与错误

- [ ] 实施：JSON统一status/revision/pending_tasks/next_action；退出码0/2/3/4/5遵循09章。
- 交付与核对：创建成功不被称成稿通过；缺Host、无效字段和冲突可分别复现。

### T04.05 自动呈现首次正文

- [ ] 实施：首次compose/create--draft/import draft有页后返回呈现动作，主Skill立即调用view--open；与T05.min接线。
- 交付与核对：自然语言执行记录中用户无需补view；无服务时如实返回原因。

### T04.06 完成缺口问答与主Skill循环

- [ ] 实施：已确认答案有效；仅核心缺口触发询问，正常任务连续accept/continue。
- 交付与核对：无额外约束与不适用可继续；默认成稿用真实D1/D2，不把精修示例当自动成绩。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-C02 场景结构与短稿 | 同源D1/D2结构/深度/结尾不同；合理短稿不加固定痛点风险CTA | `tests/rebuild/test_content.py` | 本卡关闭 |
| AC-C04 不再循环配论点 | 预置正确接口论点不能被数组取模配成公司成立年份；默认入口不调用旧enrich | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-C06 不依赖库和工作区 | 普通材料目录、无历史库缓存/模板可从create正常进入Host任务 | `tests/rebuild/test_service_flow.py` | 本卡关闭 |
| AC-C07 不重复问答 | 无额外约束/不适用有效；只问足以改变本次决定且来源无法回答的缺口 | `tests/rebuild/test_content.py` | 本卡关闭 |
| AC-S14 临时结果完整往返 | compose/blueprint/reconstruct/review/repair信封kind/files/refs一致；真实文件先存再原子采用；重复幂等、旧输入/路径逃逸拒绝 | `tests/rebuild/test_tasks.py` | 本卡关闭 |

阶段命令、测试用例责任和正反例见[阶段交接与验证](handoffs.md)。先运行本阶段用例，完整模块仅在整卡范围已实现后运行；缺测试不得用skip代替。

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：正文无机制回方法，不以schema加字段求专业。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：待填
已完成子任务 / min或整项：待填
实际命令与结果：待填
AC证据与未验项：待填
下游接收接口 / 文件 / 版本：待填
阻碍与下一动作：待填
```
