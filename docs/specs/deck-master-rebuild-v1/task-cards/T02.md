# T02｜五对象与最小原子存储

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

实现五schema/跨引用和不可变对象、current原子提交、基本恢复；增加Document.change幂等记录，不上数据库平台；明确design_context与nullable原始来源hash、asset Ref；基础call_allowance事务字段一起落地。

- 所属工作包：[WP01](../work-packages/WP01.md)。
- 最早启动：T01边界/目标身份确定。
- 整项完成依赖：T01。
- 早期交付：T02.min：五对象最小合法子集、不可变blob与原子指针、基础幂等/取消保护。
- 必读规范：[01-target-architecture.md](../specs/01-target-architecture.md)、[03-data-and-state-contracts.md](../specs/03-data-and-state-contracts.md)、[08-edits-recovery-and-budget.md](../specs/08-edits-recovery-and-budget.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。
- 输入样例：[完整往返例](../examples/roundtrips/README.md)及[五份拟实施契约](../contracts/)。示例只用于协议对照，不是实际通过记录。

## 修改范围与接口

store.commit_change(project, base_revision, read_set, writes, cancelled_task_check)；current 仅指向完整不可变 Document。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/__init__.py`
- `src/deck_master/__main__.py`
- `src/deck_master/models.py`
- `src/deck_master/store.py`
- `src/deck_master/resources/contracts/`
- `tests/rebuild/conftest.py`

本卡同时负责新增 `tests/rebuild/test_contracts.py`，覆盖五对象正例和跨引用反例；阶段用例见交接表。关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T02.01 建立五对象和包骨架

- [x] 实施：落地Document/Page/Artifact/Task/Review及Ref校验、最小__init__/__main__与合成项目fixture；schema从包资源读取。
- 交付与核对：五个合法示例可读，坏引用/重复页/跨项目引用给具体路径；不含第六永久对象。

### T02.02 实现不可变字节存储

- [x] 实施：统一JSON canonical编码与媒体字节hash；先staging再objects；文件Ref按项目相对位置解析。
- 交付与核对：同字节复用；已有对象不能被不同字节覆盖；路径逃逸和跨根符号链接拒绝。

### T02.03 实现原子current提交

- [x] 实施：store.commit_change在项目flock内重读current、检查read_set/取消/operation，再写Document并原子换指针。
- 交付与核对：对象半写、Document已写指针未切、指针已切响应丢失分别恢复完整旧版或新版。

### T02.04 落地制作配置与调用字段

- [x] 实施：校验design_context、nullable original_sha256、asset Ref及Task.call_allowances；默认值只在创建时持久化。
- 交付与核对：空原hash可表示未知，媒体实hash仍必填；配置未知引用报错，无隐藏默认。

### T02.05 交付T02.min并补完整故障边界

- [x] 实施：先交五对象最小可用集/原子指针/基础幂等取消，后续扩展不另建min格式。
- 交付与核对：T03/T04能接入；全卡关闭前完成AC-S01/S02并交故障注入结果。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-S01 事务不半写 | 对象/Document/指针各断点故障，当前仍完整旧版或完整新版 | `tests/rebuild/test_store_transactions.py` | 本卡关闭 |
| AC-S02 文件安全 | 路径逃逸/符号链接危险写入/坏对象不能覆盖源和历史 | `tests/rebuild/test_store_transactions.py` | 本卡关闭 |

阶段命令、测试用例责任和正反例见[阶段交接与验证](handoffs.md)。先运行本阶段用例，完整模块仅在整卡范围已实现后运行；缺测试不得用skip代替。

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [x] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [x] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [x] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：对象格式/事务未成立先修，不接真实生图或UI写入。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：ZCode rebuild session / T02 提交（见 git log）
已完成子任务 / min或整项：T02.01–T02.05 全部；整项交付（T02.min 已含在整项内）
实际命令与结果（候选解释器 /tmp/deck-rebuild-dev/bin/python，3.12）：
  - pytest test_contracts.py::test_five_valid_objects / ::test_invalid_references_report_paths → 2 passed
  - pytest test_store_transactions.py::test_atomic_current_failure → 3 passed（objects/document/pointer 三个故障点）
  - tests/rebuild 全目录 18 passed
AC证据与未验项：AC-S01（三断点注入+读集冲突）、AC-S02（路径逃逸/符号链接/坏对象拒写拒读）已关闭；
  未验项＝真实生图/UI写入未接（按卡禁止）；跨引用恢复矩阵中读集重基归 T12
下游接收接口 / 文件 / 版本：
  - src/deck_master/models.py：validate_*_semantics、new_document、default/normalize_design_context、canonical_json_bytes
  - src/deck_master/store.py：Store.put_blob/put_json_object/read_object_*、load_document/commit_change(init_project)
  - resources/contracts/ 五 schema（包资源真源）
阻碍与下一动作：无阻碍；T03（sources/content）与 T04（tasks/service/cli）按 start_after 可开始
