# T14｜完整四视图工作台

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

真实页/蓝图/SVG/PPT、反馈、正文更新、取消、冲突；无Host时明确等待；loopback必要安全；自然语言入口无需手动view；同项目URL复用、无浏览器/服务失败真实返回；显示编辑能力范围。

- 所属工作包：[WP03](../work-packages/WP03.md)。
- 最早启动：T05,T11,T12。
- 整项完成依赖：T05,T11,T12。
- 早期交付：按整项任务依赖执行。
- 必读规范：[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)、[10-review-workbench.md](../specs/10-review-workbench.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

10.3 GET/POST API 全部通过 service/store；编辑 expected_revision/page_hash/operation_id；同页冲突 409。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/web.py`
- `src/deck_master/view.py`
- `src/deck_master/resources/static/`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T14.01 完成真实四视图阅读

- [ ] 实施：复用DESIGN.md布局，页列表/正文/蓝图/SVG/PPT/问题/历史直接消费ProjectView。
- 交付与核对：无preview有两页即两页；失败和过期产物能打开且版本清楚。

### T14.02 实现正文编辑与视觉反馈

- [ ] 实施：POST content/edits带revision/page_hash/operation，正文结构化编辑，区域意见形成Host任务。
- 交付与核对：修改从Page开始；按钮创建awaiting_host，无Host不假running。

### T14.03 接取消检查导出恢复

- [ ] 实施：API统一service，409/400或422/503带具体错误；表单冲突保输入。
- 交付与核对：取消晚到不更新；恢复生成新revision；导出仍按用途判断。

### T14.04 完成真实服务与文件安全

- [ ] 实施：loopback、Origin/token、登记Artifact ID访问；材料用textContent，SVG隔离展示。
- 交付与核对：正常只读无需账号；路径逃逸/XSS/跨源写入反例拒绝。

### T14.05 完成桌面入口连续交互

- [ ] 实施：自动view、健康服务复用，无浏览器/服务启动失败分流；更新给当前页链接。
- 交付与核对：自然语言任务不需用户补开UI；工作台反馈能被下一Host真实执行。

### T14.06 验收布局和编辑能力说明

- [ ] 实施：1280×800/1440×900、键盘切页/反馈/取消、对照缩放；显示实际画布/字体替代。
- 交付与核对：浏览器操作记录与截图齐备；形状文字编辑不说成Office图表编辑数据。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-U01 真实页数 | 无preview_manifest也显示Document真实页，不出现可交付+0页+初始化问卷 | `tests/rebuild/test_workbench_e2e.py` | 本卡关闭 |
| AC-U02 当前版本统一 | CLI/UI/continue/export读同一revision与检查，修改后一起更新 | `tests/rebuild/test_web.py` | 本卡关闭 |
| AC-U03 本地服务 | 自然语言主Skill首次正文接收后自动view --open；有效服务/URL复用；无浏览器返回真实本地地址，服务失败为null+原因；不让用户补命令 | `tests/rebuild/test_web.py` | 本卡关闭 |
| AC-U04 四视图 | 原文/蓝图/SVG/PPT显示真实文件和版本，问题能跳转对象/区域 | `tests/rebuild/test_workbench_e2e.py` | 本卡关闭 |
| AC-U05 无Host不假运行 | UI提交反馈后awaiting_host，实际start/accept才更新执行与成品 | `tests/rebuild/test_workbench_e2e.py` | 本卡关闭 |
| AC-U06 写接口安全 | 同源token/路径白名单/XSS处理；只读浏览不要求团队账号 | `tests/rebuild/test_web.py` | 本卡关闭 |
| AC-U07 可用布局 | 1280/1440下导航、比较、反馈可用，键盘可操作，状态不只颜色 | `tests/rebuild/test_workbench_e2e.py` | 本卡关闭 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_web.py tests/rebuild/test_workbench_e2e.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：UI实际数据缺失先修service，不能用演示假卡片。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：待填
已完成子任务 / min或整项：待填
实际命令与结果：待填
AC证据与未验项：待填
下游接收接口 / 文件 / 版本：待填
阻碍与下一动作：待填
```
