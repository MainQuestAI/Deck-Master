# T05｜最小真实视图与隔离包

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

最小页面列表/正文/文件链接直接读Document；隔离安装验证方法/schema资源，尚不激活用户当前版本；先同项目服务复用/实际URL/四视图槽位及失败稿可看，随后隔离资源验证。

- 所属工作包：[WP01](../work-packages/WP01.md)。
- 最早启动：T02.min,T04.min。
- 整项完成依赖：T02,T04。
- 早期交付：T05.min：只读四视图/实际页集合/自动view；资源隔离检查随后补全。
- 必读规范：[01-target-architecture.md](../specs/01-target-architecture.md)、[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)、[10-review-workbench.md](../specs/10-review-workbench.md)、[11-package-install-and-cutover.md](../specs/11-package-install-and-cutover.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

view.project_view(project, revision=None)；GET project/pages/artifacts；view --open 返回实际 review_url/view_status。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/view.py`
- `src/deck_master/web.py`
- `src/deck_master/resources/static/`
- `pyproject.toml`
- `setup.py`
- `tools/build_hook.py`
- `MANIFEST.in`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T05.01 建立Document只读视图

- [ ] 实施：view.project_view从current解析页数组、正文、Artifact/Review；零页和失败稿分别显示。
- 交付与核对：删除旧preview_manifest仍显示真实页数；当前与历史版本不混淆。

### T05.02 建立最小工作台和本地服务

- [ ] 实施：web只读API、真实四视图槽位及文件链接；按DESIGN.md复用现有可用样式。
- 交付与核对：有正文即能查看；蓝图/SVG/PPT缺失槽位明确等待而非假预览。

### T05.03 接入自动打开和服务复用

- [ ] 实施：实现view--open健康检查、有效URL复用、无浏览器返回实际URL、服务失败null。
- 交付与核对：T04首次正文自动出现工作台；后续继续不反复弹窗或新起服务。

### T05.04 配置src隔离构建

- [ ] 实施：pyproject、setup.py、MANIFEST.in和build_hook仅打包新包及唯一Skill源资源；importlib.resources读取。
- 交付与核对：隔离wheel可加载schema/method/static，不借源码路径；不激活用户current。

### T05.05 交付T05.min和后续资源验证

- [ ] 实施：先只读四视图联通，再补普通pip/sdist构建链资源检查。
- 交付与核对：早期可供T10实物对照；完整资源与包边界最终仍由T16/T20复验。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-U01 真实页数 | 无preview_manifest也显示Document真实页，不出现可交付+0页+初始化问卷 | `tests/rebuild/test_workbench_e2e.py` | T14关闭，本卡交接 |
| AC-I01 包依赖边界 | 新src不import旧命名空间；compiler不反向依赖业务审批 | `tests/rebuild/test_package_boundary.py` | T16关闭，本卡交接 |
| AC-I06 资源完整 | wheel与sdist/pip install路径包含方法/schema/static；不借checkout补文件 | `tests/rebuild/test_install.py` | T20关闭，本卡交接 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_install.py tests/rebuild/test_package_boundary.py tests/rebuild/test_workbench_e2e.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：依赖旧preview则修view；源码偷读资源则修打包。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：待填
已完成子任务 / min或整项：待填
实际命令与结果：待填
AC证据与未验项：待填
下游接收接口 / 文件 / 版本：待填
阻碍与下一动作：待填
```
