# T01｜冻结现场与逐文件处置

状态：**待开发，未运行验收**。负责人：待领取。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

读取B0/K0、本地安装和未提交变更；生成全Git路径处置表；记录事故完成叙述更正；建立新包反向import禁区；仅建立处置清单/引用基线，不要求此时旧引用归零。

- 所属工作包：[WP01](../work-packages/WP01.md)。
- 最早启动：—。
- 整项完成依赖：—。
- 早期交付：固定来源/清单，当前旧代码和用户资料不删。
- 必读规范：[00-execution-baseline.md](../specs/00-execution-baseline.md)、[01-target-architecture.md](../specs/01-target-architecture.md)、[02-file-change-and-retirement.md](../specs/02-file-change-and-retirement.md)、[12-legacy-and-document-retirement.md](../specs/12-legacy-and-document-retirement.md)、[14-agent-execution.md](../specs/14-agent-execution.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

固定 B0/K0 SHA 与当前 checkout 映射；逐文件处置以实际 Git 路径为键。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `docs/specs/deck-master-rebuild-v1/inventory/`
- `AGENTS.md`
- `docs/planning/`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T01.01 核验开发与提取基线

- [x] 实施：记录当前分支/SHA/工作树、B0和K0可解析性、当前安装解释器与模块来源；对B0至当前的差异补充映射。
- 交付与核对：基线记录能区分源码、远端、安装版本；不改已有历史。

### T01.02 展开完整文件处置表

- [x] 实施：运行只读清单工具；关联old-files/new-files/contracts/tests/skills/root配置；未知路径逐项注明用途与处置。
- 交付与核对：全部跟踪路径有保留、提取、重写、归档或退役去向，用户资料单列不删除。

### T01.03 固定提取边界

- [x] 实施：标记新包禁用旧命名空间及compiler反向依赖；给每项提取标B0/K0文件与SHA。
- 交付与核对：后续开发可直接找到实现来源和行为测试，不整包合并K0。

### T01.04 更正事故与旧完成描述

- [x] 实施：定位把对象存在、两reviewer名称或旧pass写成成功的活动文档；保留原证据，增加更正与新Spec入口。
- 交付与核对：更正清单区分真实工程结果与尚未验证的还原能力；本卡不要求旧引用归零。

### T01.05 准备固定制作输入

- [x] 实施：按inputs.md建立三类公开或合成样本及本机映射；记录Page版本、原图、来源和独立视觉期待，原事故材料可用性单列。
- 交付与核对：接收者能打开输入并核对hash；缺原事故时明确替代样本，不宣称事故复跑。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-L07 建立退役清单 | T01只冻结逐路径处置/引用基线与保留项；此时不要求AC-L05归零，后者由T24完成 | `tests/rebuild/test_package_boundary.py` | 本卡关闭 |

阶段命令、测试用例责任和正反例见[阶段交接与验证](handoffs.md)。先运行本阶段用例，完整模块仅在整卡范围已实现后运行；缺测试不得用skip代替。

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [x] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [x] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [x] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：本地不可解析基线则只修映射，不reset工作树、不整包合并K0。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：ZCode rebuild session / T01 提交（见 git log）
已完成子任务 / min或整项：T01.01–T01.05 全部；整项交付（本卡无 min 切片）
实际命令与结果：
  - git rev-parse 2a866cf/2c5a4c5 均可解析；K0 承载分支 origin/codex/sc1.1-native-core
  - python3 tools/materialize_inventory.py --repo . --ref 2a866cf1… --out /tmp/deck-rebuild-inventory
    → 931 跟踪文件全部有处置去向；unclassified=[]；repo_modified=false
  - /tmp/deck-rebuild-dev/bin/python -m pytest -q tests/rebuild/test_package_boundary.py → 3 passed
AC证据与未验项：AC-L07 已关闭（本卡）；未验项＝本机无旧安装（I0 来源按未知登记）、样本原图 pending
下游接收接口 / 文件 / 版本：inventory/baseline-record.md（提取边界表 B0/K0 SHA）；$DECK_REBUILD_EVIDENCE/inputs/index.json（三样本 hash）；AGENTS.md 重建基线节
阻碍与下一动作：无阻碍；T02 可开工（建立 src/deck_master/models.py、store.py 与五 schema 资源），T08 可按固定来源读 B0/K0
