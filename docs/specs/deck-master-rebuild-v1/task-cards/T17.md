# T17｜唯一默认入口与文档

状态：**整卡完成（2026-09-21）；AC-I04 关闭,AC-C06 交接 T04**。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

新旧命令明确映射/退役，manifest/README/installer/UI共同解除PPTMaster与Library前置。

- 所属工作包：[WP04](../work-packages/WP04.md)。
- 最早启动：T15,T16。
- 整项完成依赖：T15,T16。
- 早期交付：按整项任务依赖执行。
- 必读规范：[09-cli-and-host-protocol.md](../specs/09-cli-and-host-protocol.md)、[11-package-install-and-cutover.md](../specs/11-package-install-and-cutover.md)、[12-legacy-and-document-retirement.md](../specs/12-legacy-and-document-retirement.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

console entry=deck_master.cli:main；09.6 旧命令映射；新写入口先识别旧格式，不能就地初始化。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/cli.py`
- `skills/deck-master/`
- `product-capability-manifest.json`
- `README.md`
- `AGENTS.md`
- `docs/agent-task-index.md`
- `docs/agent-recovery-playbook.md`
- `docs/migration-to-rebuilt-core.md`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T17.01 清点并映射全部旧命令

- [x] 实施：将只读CLI清单逐项对应别名或退役提示；import-plan/build/next-step等按09.6落实。
- 交付与核对：每个旧入口有明确行为，不能继续调用旧main隐式生成规则稿。

### T17.02 统一默认新入口

- [x] 实施：console entry、__main__、根Skill、API和安装器指新service；隔离候选先验。
- 交付与核对：没有永久v2开关，用户主任务走宿主成稿。

### T17.03 解除所有绑定宣传与检查

- [x] 实施：manifest/doctor/routing/README/UI不再要求PPTMaster SHA认证或Library安装。
- 交付与核对：无配置无缓存仍进入新流程；真实python-pptx/renderer依赖正常报告。

### T17.04 迁移使用和恢复文档

- [x] 实施：更新AGENTS/任务索引/恢复说明/主README和migration文档，历史规格标归档。
- 交付与核对：新用户照文档走create→view→edit→export，旧run不会被自动写新pointer。

### T17.05 做入口矩阵回归

- [x] 实施：新命令、旧别名、旧run误输入、退役命令逐类验证。
- 交付与核对：JSON/退出码/提示一致；实际默认激活由T25完成。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-I04 唯一入口 | 根Skill/CLI/API/doc/installer指同一路；旧命令明确映射或退役 | `tests/rebuild/test_cli.py` | 本卡关闭 |
| AC-C06 不依赖库和工作区 | 普通材料目录、无历史库缓存/模板可从create正常进入Host任务 | `tests/rebuild/test_service_flow.py` | T04关闭，本卡交接 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_cli.py tests/rebuild/test_service_flow.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [x] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [x] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [x] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：不能保留永久v2开关把旧默认留在原处。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：Kimi Code（工作树待提交：cli.py legacy 层、pyproject scripts、README/AGENTS/三文档、新建 test_cli.py 与 migration 文档）
已完成子任务 / min或整项：T17.01–T17.05 全部完成，整卡关闭
实际命令与结果：.venv/bin/python -m pytest -q tests/rebuild/test_cli.py tests/rebuild/test_service_flow.py → 40 passed；tests/rebuild 全量 333 passed（309+24）；`.venv/bin/deck-master --version` = 1.0.0.dev2
AC证据与未验项：AC-I04 = test_cli.py 24 项（7 别名/18 指引/60+4 退役逐类断言、legacy_map 全覆盖、旧 run 格式拒绝不就地初始化、-m 同入口、wheel entry_points 断言）；AC-C06 交接 T04（T04 既有测试+本卡 CLI 层同义证据）。未验项：实际默认激活归 T25
下游接收接口 / 文件 / 版本：deck_master.cli:main 唯一入口（legacy _legacy_dispatch 绝不 exec 旧 main）；`deck-master legacy-map` 全表
阻碍与下一动作：旧套件 17 个失败测试（tests/test_skill_installation.py 等）归 T24 退役处置；product-capability-manifest.json 未改字节（其退役归 T24）。下一步 T19/T18
```
