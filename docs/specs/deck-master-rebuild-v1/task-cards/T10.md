# T10｜真实渲染与首段完整闭环

状态：**工程 AC（K01/K08/K10/K11/K13/K14）已关闭（2026-09-20）；AC-B04/V07 已补实际图形与历史切片核对；P1 十页 Page、蓝图、SVG 和正式原生 PPTX 已完成工程链路验证，整卡未关**。AC-V02 的“主链刚可用时”历史时间条件无法事后补证，仍须如实标记。P1 材料已收到，原“待用户到料”是 2026-09-20 的历史状态。优先顺序以[任务总览](README.md)的早期切片与完整依赖为准。

## 目标与输入

事故型三页在小UI完整显示，使用真实原图/重构/渲染；同时尽早运行P1，内容与制作证据分开；先在事故页实际子集完成min三方对照，不等所有材料格式/绘制修复；整体完成再覆盖声明子集。

- 所属工作包：[WP02](../work-packages/WP02.md)。
- 最早启动：T05.min,T07.min,T08.min,T09.min。
- 整项完成依赖：T05,T07,T09。
- 早期交付：T10.min：真实PPT、原图/SVG/PPT实际阅图，四视图可见。
- 必读规范：[05-blueprint-and-svg.md](../specs/05-blueprint-and-svg.md)、[06-native-compiler.md](../specs/06-native-compiler.md)、[10-review-workbench.md](../specs/10-review-workbench.md)、[13-verification-and-release.md](../specs/13-verification-and-release.md)、[14-agent-execution.md](../specs/14-agent-execution.md)。
- 原始施工依据：[任务清单](../inventory/task-list.csv)、[新文件台账](../inventory/new-files.csv)、[旧文件处置](../inventory/old-files.csv)、[验收矩阵](../inventory/acceptance-matrix.csv)。

## 修改范围与接口

render_deck(pptx_path, output_dir, options)；readback(pptx_path, inputs, expected_content, expected_relations)。

主责路径（仓库相对路径，未建文件为拟建目标）：

- `src/deck_master/compiler/render.py`
- `src/deck_master/compiler/readback.py`
- `src/deck_master/production.py`

关联测试路径见下方验收表。共享文件按[职责分界](README.md#共享文件与接线责任)协调；本卡不得顺带重写相邻产品流程。

输入由 T01.05 准备，具体样本和接收条件见[输入交接](inputs.md)。AC-K01 在 T05 隔离包和 T09 实际绘制完成后验收。

## 开发子任务

这些是本卡负责人的内部步骤；跨卡提前交接以[阶段交接与验证](handoffs.md)为准，不逐项作为独立任务派发。

### T10.01 接真实编译和双段渲染

- [x] 实施：production调用纯compiler；SVG与PPT各自用真实工具渲染；当前文件写入新Artifact。
- 交付与核对：四视图显示正文/源图/SVG/PPT真实版本；缺renderer明确未验证。

### T10.02 实现实际文件读回

- [x] 实施：从PPT ZIP/XML读取文字、端点、path/字体/画笔，对照原Page和SVG而非自产trace。
- 交付与核对：删职责、反箭头、隐藏1pt/透明全文均能定位真实异常。

### T10.03 完成首张复杂事故页还原

- [x] 实施：按原图保留模块/图标/状态/层级/辅助说明，分别阅图像→SVG和SVG→PPT。
- 交付与核对：T10.min交真实可编辑PPT及三方对照、问题记录；不能只凭对象数量完成。

### T10.04 扩展三页与布局敏感性

- [x] 实施：覆盖架构职责、图表/表格、另一方案页；同业务换不同布局原图，几何随之改变。
- 交付与核对：三页正文/图形完整；换图不只是换hash，简化版不能冒充还原。

### T10.05 尽早运行P1迁移

- [ ] 实施：主链一旦可用用陌生材料生成首稿，记录业务缺陷并回内容方法或制作层。
- 交付与核对：P1用后归开发；精修制作样例与默认内容能力分开。

### T10.06 完成声明子集与设计一致性

- [x] 实施：补全06.2特性、4:3/字体/Logo、视图比例和实际读回；按依赖补全T03/T09范围。
- 交付与核对：完整卡关闭须覆盖AC-K13/K14，min成功不等于所有格式/特性完成。

## 验收操作与证据

从下表构造输入或实际操作，观察“必须结果”。测试文件名是拟落点，**不是已存在或已通过的证明**；行为需要实际Host、阅图、浏览器或人类时，自动化只证明其工程部分。

| AC | 必须结果／反例 | 验证落点 | 完成归属 |
| --- | --- | --- | --- |
| AC-K01 独立转换 | 只给SVG/资产/参数，隔离安装可产PPT，不读旧OS/审批/HOME仓库 | `tests/rebuild/test_compiler_api.py` | 本卡关闭 |
| AC-K08 实际几何读取 | 从输入SVG+独立Page关系检查实际PPT/XML，不只trace对trace | `tests/rebuild/test_readback.py` | 本卡关闭 |
| AC-K10 可读性 | 1pt/透明补全文层不能过内容覆盖；真实文本溢出有定位 | `tests/rebuild/test_readback.py` | 本卡关闭 |
| AC-K11 实际渲染 | SVG/PPT分别实际渲染，缺renderer标未验证，旧预览不能充新图 | `tests/rebuild/test_render.py` | 本卡关闭 |
| AC-K13 支持范围 | 线/路径/图标/表格/图片/渐变和画布按子集正确；其余明确限制 | `tests/rebuild/test_drawingml.py` | 本卡关闭 |
| AC-B04 图像实际参与 | 同业务两张显著不同布局源图，SVG几何相应变化，不能只换hash | `host_image_review` | 本卡关闭 |
| AC-V02 P1提前迁移 | 主链刚可用就运行陌生业务材料，用于改方法后归开发 | `manual_protocol` | 本卡关闭 |
| AC-K14 画布与设计权威 | 同一Document的4:3/指定物理尺寸、style_ref、字体和Logo被prompt/编译/UI一致采用；无隐藏16:9默认 | `tests/rebuild/test_compiler_geometry.py` | 本卡关闭 |
| AC-V07 最早真实纵向切片 | T03全格式/T09全特性尚未完成也可T10.min真实制作+只读四视图+T12.min单页改；实际阅图必做，不能宣称全产品完成 | `manual_protocol` | 本卡关闭 |

实现后从仓库根目录执行的定向测试（本次拆卡未执行产品测试）：

```bash
<候选Python> -m pytest -q tests/rebuild/test_compiler_geometry.py tests/rebuild/test_drawingml.py tests/rebuild/test_readback.py tests/rebuild/test_render.py
```

每次完成记录：源码 SHA、实际命令/输入、结果、失败或未执行原因、关联 AC、产物/截图/读回证据。客户原件、运行目录、调用私密记录留仓库外；不能用测试数量替代下表行为。

## 完成条件与失败去向

- [ ] 子任务已完成，实际新增/修改/提取/退役路径和源版本已记录。
- [ ] 本卡主责 AC 均有当前真实证据；共享 AC 的后续关闭任务已有可消费输入。
- [ ] 早期切片与完整范围分别说明；未完成最终依赖时只交付切片，不把整个任务勾完。

失败返回：分段找第一丢失点；不先增加平台和评分；P1改方法后归开发。保留失败输入与当前旧产物，修最早出错的层；不得为当前坏结果修改验收答案。

## 交接回填

```text
负责人 / 实施提交：Kimi Code（工作树待提交：svg.py node/edge ref 捕获 + pipeline.readback 扩展 + 5 个新测试模块）
已完成子任务 / min或整项：T10.01/02/03/04/06 完成；T10.05（P1）待用户陌生材料，整卡未关
实际命令与结果：.venv/bin/python -m pytest -q tests/rebuild/test_compiler_geometry.py tests/rebuild/test_drawingml.py tests/rebuild/test_readback.py tests/rebuild/test_render.py tests/rebuild/test_compiler_api.py → 20 passed；tests/rebuild 全量 213 passed（193+20）
AC证据与未验项：AC-K14=test_compiler_geometry（4:3 Document 走真实 produce 全链，PPT sldSz=9144000×6858000 EMU，prompt 含 canvas 不含 1280x720，默认画布仅创建期）；AC-K08+AC-K10=test_readback（7 项：edges/nodes 正向、reversed_arrow 定位、missing_node/edge、undirected 仅查存在、全文透明/<6pt 收紧判定、局部装饰豁免、slide 越界定位；svg.py 捕获 data-node-ref/data-edge-ref）；AC-K11=test_render（rsvg 与 soffice+pdftoppm 真实渲染 PNG 语义断言，缺 renderer 经 NeedsTool 标未验证）；AC-K13=test_drawingml（线/路径/图标/表格形状组/图片/渐变/画布结构 + 子集外显式失败）；AC-K01=test_compiler_api（HOME/cwd/env 隔离、中途改字节拒绝、失败不发布）。未验：AC-B04（两张同业务异布局源图 Host 阅图）、AC-V02（P1 陌生材料，用户阻塞）、AC-V07（min 证据见 2026-09-16 记录）
下游接收接口 / 文件 / 版本：deck_master.pipeline.readback（新 finding：missing_node/missing_edge/reversed_arrow/overflow_bounds；hidden_or_tiny_text 收紧为全文级）；IR 形状含 node_ref/edge_ref
阻碍与下一动作：P1 陌生材料与受众用途（用户输入）阻塞 T10.05/AC-V02；AC-B04 需 Host 阅图安排。T10 其余部分关闭后 WP02 仅余 T07 整卡与 T11/T12 接线
```

### 2026-09-20 工程 AC 关闭记录

新建 5 个测试模块（20 项，全部经真实 compile_deck/produce 与真实渲染工具链）：test_compiler_geometry（画布权威）、test_drawingml（支持范围矩阵）、test_readback（几何读取与可读性）、test_render（双段真实渲染）、test_compiler_api（独立转换）。扩展 svg.py 捕获 data-node-ref/data-edge-ref 进 IR；扩展 pipeline.readback：missing_node/missing_edge/reversed_arrow（首末点与节点 bbox 中心比对，直边强语义、曲线边弱语义已记 limitation）、全文级 hidden_or_tiny_text（局部装饰豁免）、slide 越界定位。未发现需修复的既有 bug；现有行为全部回归保留。


### 2026-09-24 前置证据更新

P1 陌生业务材料已收到，首次 `create` 揭示中文源文件名导致非法 `source_id`；修复后 10 页正文已接收，首张蓝图经真实图像工具生成并登记。此阶段只证明 P1 已启动及发现一项开发缺陷；完整 SVG/PPT 与业务内容缺陷归因待继续核对。AC-V02 要求的“主链刚可用时”无法事后补做时间证据。原件与运行证据保存在仓库外，见 T21 对照方案的阶段记录。

同日后续补验：AC-B04 使用同一合成售后业务页的横向原图与新生成纵向原图，原生 SVG 的“服务点→助手→专家”坐标由横向跨度 1227px、纵向跨度 0px 变为横向 0px、纵向 516px；主线程实际并排阅图并核对 120/54/45% 及只读接口，记录在仓库外 `validation/t10-b04-20260924/ac-b04-host-review.json`。AC-V07 复核 2026-09-16 的真实原图/SVG/PPT 双段预览、浏览器工作台与单页数值修改记录，首轮原图误配保留为失败，纠正版作为早期切片通过证据；记录在仓库外 `validation/t10-v07-20260924/ac-v07-host-audit.json`。两项均不代表陌生业务 P1 或专业内容验收。

P1 随后按真实 Host 任务逐页制作：第 1–5 页蓝图及原生 SVG 已接收。第 4 页蓝图凭空出现销售额 1,200/1,500 与 -20% 降幅；原始 RFP 未提供这些数字。对应 SVG 已明确标成示例数据；`src/deck_master/production.py` 的蓝图提示词补上不得虚构数值、日期、客户数据和结果的约束，`tests/rebuild/test_production.py` 定向 11 项通过。此项属于蓝图生成/制作方法缺陷，不能把示例数值写成客户事实。原始图、SVG、并排阅图与缺陷归因均保存在仓库外 P1 evidence 目录。

第 6 页首次 ImageGen 返回 `connection failed: error sending request`，最初将 allowance 记为 `unknown` 并暂停。随后查到本次会话的工具事件为已执行且终态失败，按 08.6 将该次修正为 `consumed`；这是 Host 事件证据，不是供应商计费证明。通过已有事务服务新增 `task call allocate` CLI 入口后分配 `call-2`，重试成功，原图与调用报告已保存在仓库外 `evidence/p06-production-20260924/`，蓝图任务已接收。第 6 页 SVG 及后续页仍待验收。

第 1–5 页先做独立原生 PPTX 编译样本。原 SVG 中的 PingFang SC 在本机 PDF 渲染时替换成 STKaitiTC，字体效果不合格；改用本机可用的 Arial Unicode MS 后，五页 PPTX 结构回读 0 项发现。编译器同时修复完整对角渐变转换和靠近画布边缘的文本框冗余越界。随后正式项目接收 p01–p05 字体修正版，以及 p06–p07 删除子集外文本描边属性的修正版；原对象留在历史中。

P1 十页的 Page、ImageGen 蓝图和原生 SVG 均已接收，正式项目编译、渲染和读回十页原生 PPTX：`render_report.status=pass`，0 项 findings。工程阅图首次接收 60 个维度后，发现 SVG 依赖键映射不一致，导致其中 30 个维度被误判过期；修正 `editing._current_artifact_digests` 并补定向测试，再接收 30 个受影响维度。最终 `final-readiness.status=ready`、`review_status=pass`，review export 可查看。该工程结论不包含专业内容审阅、目标桌面编辑或客户验收；第 4 页数值仍只能作为明确标注的示例数据。完整过程索引和缺陷归因保存在仓库外 `evidence/T10-progress-index-20260924.md` 与 `evidence/p1-issue-log-20260924.json`。

### 2026-09-16 单页 min 交付

详见 [单页交付记录](../single-page-delivery-20260916.md)。本页已实际输出 SVG、可编辑 PPTX、渲染与回读证据；原始全卡子任务及 AC 未据此全部勾选。
