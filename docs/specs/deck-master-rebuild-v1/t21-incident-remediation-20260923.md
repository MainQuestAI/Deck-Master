# T21 Host 续跑与文件交付修复

本记录对应 2026-09-23 T21 首次制作事故。首次 compose 和失败产物是开发证据，不能替换成修复后的成绩。客户材料、运行目录和调用记录留在仓库外。

| 已确认的问题 | 修复 | 关闭证据 |
| --- | --- | --- |
| `continue` 只返回下一步，Host 可忽略并转向外部排版 | 项目 Skill 明确收到 `next_action` 后持续处理当前项目；最终附件前调用 `handoff-check` | 原会话在相同 D1/D2 项目中实际续跑，若停下须给出具体工具、输入或用户决定阻碍 |
| CLI 导出有状态约束，但回复可附上不属于当前项目的 PPTX | 新增只读 `handoff-check`，核对拟附文件 hash 与当前 `outputs.pptx`、全部页槽位、渲染报告和待执行任务；delivery 另核当前工程审阅 | 外部 PPTX 和未完成项目返回 blocked/exit 3；当前导出文件通过；Host 实跑最终附件能回查同一项目 |
| 旧调度先补完全部蓝图，再开始第一页 SVG，推迟了还原反馈 | `continue` 改为每页蓝图后先派发该页 SVG，再开始下一页蓝图；整套 PPT 仍在所有页完成后编译 | 两页反例验证第一页有蓝图后优先派发第一页 SVG；真实 D1/D2 再验证生产顺序 |

这三项只能证明可检查的代码边界。Host 仍可不调用 CLI，因此必须用原会话、原 D1/D2 项目做一次真实续跑；提示词明确本次新稿优先于任何文字预览或旧稿选编，并要求每次 `task accept` 后消费下一条 `continue`。T21 A/B/C 正式冻结仍按任务卡独立运行，不将这次修复运行补写为第一次成功。
