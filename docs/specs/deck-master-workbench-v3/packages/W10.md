# W10 · 完整运行台与恢复

状态：设计提案，未实施、未验收。执行基线：d1c7c4600cb0fc781070116ed8cc8a1ff6b8b7ca；产品真相见 [PLAN](../PLAN.md)，当前实现见 [CODE-BASELINE](../CODE-BASELINE.md)。

## 用户结果

回到工作台能分清轮到自己还是Host、哪些页返回、哪里失败，以及继续做哪一步；断线和半完成不会丢工作。

## 范围与不做

开发/Host接入规范：遵循 [DX-SPEC](../DX-SPEC.md) §3任务/请求查询、§4调用账、§5恢复、§6文档及§7未知调用/CLI边界。所有v3新增命令/接口均为本卡待实现提案，不是当前可运行能力。

在W06已有最小交接/取消/重放上，完成按ChangeSet归组的全生命周期、真实接手时间、部分结果/候选、任务分页、超时、冲突及批量恢复中心；用户待办与系统运行分开。

不做：重新实现W06基础交接、轮询触发执行、自动接管超时任务、静默重试外部调用、用进度条编造模型进度。

## 依赖与拆分

依赖W07；与W08/W09可独立接入已稳的运行投影。**依赖修正**：本卡不是W07第一次能交接的前置，最小真实闭环已由W06交付；这里补完整运营面与组合恢复。

## 契约与服务复用

工程约束见 [ENGINEERING-SPEC](../ENGINEERING-SPEC.md) §2/§4/§6：operations索引可由业务历史重建，个人draft journal仅恢复UI、不作为任务真相；跨端口只自动恢复已项目ACK的草稿，未同步文件手动导入。300页×5候选×3Attempt运行20分钟，末5分钟heap相对第6–10分钟稳定中段增幅≤20%，记录基准环境与原始采样，首轮不达标修复不改门槛。沿用30分钟用户核实/取消恢复，不新增心跳调度。

按DX-SPEC§3–§7复用W02 requests/attempts查询、W06 operations/handoff查询及现有task start/status/cancel，不再另建查询账、重放身份或执行器。运行台链接真实request/attempt/call allowance/result事实；CLI与HTTP投影一致的typed error和离线docs_ref，正常awaiting_host在状态HTTP中为200，continue退出3不当失败。调用后unknown不能视为not_sent或自动重新分配额度；查询和轮询均无执行副作用。

Task全生命周期与ChangeSet关联从W01/W02/W06扩展；保留核心queued/awaiting_host/running/completed/failed/cancelled/superseded语义，UI事实描述可细化。summary活动3秒/闲置15秒/hidden暂停/focus立即刷新/失败退避最长30秒；单请求在途，写完成主动刷新。

## 源码入口

`view.py:51`、`web.py:183`、`service.py:1624,1646,1661`、`tasks.py:840,1284,1403`、`resources/contracts/task.v1.schema.json`；前端v2 run-desk/summary-poll/recovery。

## 行为验收

- **W10-AC01**：修改组显示原请求、作用页、任务及所有结果，逆序返回不串归属；复用W02 requests/attempts和W06 operations/handoff查询，真实ID贯穿CLI/HTTP，不新建调用账或第二套查询真相。
- **W10-AC02**：只把缺事实/待交接/候选待决定/冲突/未看结果列为人的待办；正常运行不是催用户处理的todo。
- **W10-AC03**：复制/预填不显示已发送或处理中；真实execution_ref及接手时间与task_start一致。
- **W10-AC04**：刷新遵守活动3秒/闲置15秒/隐藏暂停/focus立即/最长30秒退避，单请求在途且不触发continue。按ENGINEERING-SPEC§6固定压力运行20分钟，末5分钟heap相对第6–10分钟中段增幅≤20%；环境/原始采样留证，首轮失败修复不放宽。
- **W10-AC05**：30分钟未返回可查原任务/取消后重交，未确认取消不重复派发；unknown不当not_sent、不自动重新分配额度，晚到事实保留但不覆盖。可运行恢复脚本从真实回包取ID，故障注入与真实Host记录分开。
- **W10-AC06**：部分结果可逐项看和选择；重试明确哪些未完成、哪些已采用，不重复已成功外部调用。
- **W10-AC07**：刷新/崩溃/断网恢复滚动/缩放/选择/比较及草稿；项目ACK草稿由journal跨端口读取，未同步保留origin缓冲并可下载/新origin导入，不虚称自动恢复。保存未知查原operation已提交事实，UI journal不回滚或覆盖业务/调用历史。
- **W10-AC08**：无任务有当前可做动作；错误含对象/原因/动作及离线docs_ref，CLI/HTTP共享typed error与既有退出码。continue正常awaiting_host退出3、状态读取HTTP200不当失败；完整错误JSON与可运行恢复步骤随卡交付。

所有编号只有本卡为最终验收 owner。自动化、浏览器、真实 Host 与用户验收分别记录；未执行项保持未验证。

## 示例与验证交付

随实现交付可运行查询/恢复脚本，建议examples/workbench/w10_recovery.py（未来文件），从已有变更/任务回包获取实际change/task/request/attempt/operation ID。CLI展示状态、原交接及调用事实，不通过无Origin脚本写HTTP；浏览器操作用Python Playwright同源session。提供断线、响应丢失、部分返回、可控30分钟超时、取消未知和晚到结果可复制复现步骤。

交付各新错误的完整JSON、CLI退出码/HTTP状态对照及可离线打开的docs_ref，验证continue正常退出3与读取awaiting_host=200不被误判失败。恢复脚本断言unknown不重新扣额度、成功调用不重试、operation原ID保留。故障注入/浏览器trace与真实Host恢复记录分开，所依赖W02/W06接口不在本卡重复实施。

工程验证脚本须覆盖 [ENGINEERING-SPEC](../ENGINEERING-SPEC.md) 本卡对应场景，保留原始输入/响应、故障点或性能采样；本轮仅确定规范，没有相应实现通过记录。

## 失败与恢复

错误发生在对象旁，运行台集中索引；未知调用状态必须核实或明确下一次额度，不能把unknown等同未执行。恢复中的新结果继续校验基准；停止操作优先于新结果。

## 独立交付与回退

交付运行/恢复浏览器测试及故障注入矩阵，与W06职责界限清楚。隐藏完整运行台后仍可用W06基础交接；保留任务和调用事实，不回退数据库式重置。
