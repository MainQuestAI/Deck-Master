# W09 · 内容与来源调整

状态：设计提案，未实施、未验收。执行基线：d1c7c4600cb0fc781070116ed8cc8a1ff6b8b7ca；产品真相见 [PLAN](../PLAN.md)，当前实现见 [CODE-BASELINE](../CODE-BASELINE.md)。

## 用户结果

用户从材料、论证大纲到逐页稿都能改在正确位置，且知道修改将影响哪些页，而不是把所有问题塞进生图prompt。

## 范围与不做

材料登记/替换/移除/用途说明及已读取/已采用事实；内容整合与大纲编辑；正文直接改、复杂改写/合并/拆分交Host；来源导航与影响判断。

不做：重写已经存在的inputs服务、用时间戳或字数猜无影响、删除历史材料、把merge/split当页面内容简单拼接。

## 依赖与拆分

依赖W02、W06；UI壳用W03、逐页稿容器用W05。inputs与局部content_update已有能力优先复用；大纲变动扩展事务。

## 契约与服务复用

ContentPlan与Page正文分工：大纲保存页目标/章节，Page保存内容；写入带双基准关系。inputs_update保存任务/材料并派compose/input_revision；结果显式upsert/remove/page_order/impact_summary。合并拆分新page_id记录来源，旧标注不迁移。材料摘要变更先needs_reconciliation，经Host判断后才对齐。

## 源码入口

`service.py:941,998,1115,1296`、`tasks.py:664,732`、`editing.py:251`、`sources.py:291,326`；document/page/ContentPlan契约；前端v2 content-sources。

## 行为验收

- **W09-AC01**：材料上传/提取完成与Host已读取并判断影响分别显示；材料或受众/用途/既有决定更新尚未采用时明确待协调，不要求重复确认已提供事实。
- **W09-AC02**：从正文来源可到确切材料版本及页码/段落；只到材料级时诚实说明，不虚构精确定位。
- **W09-AC03**：鼠标与键盘均可重排，焦点/选择跟随稳定page_id，未改页产物保持；移除前显示影响，完成后定位邻页并使相关未完任务失效，历史页与材料不删除，整稿PPT不冒充最新。
- **W09-AC04**：merge/split返回新身份和来源关系；显式选定多页交Host跨页改写时保留原page_id及来源，未选页ref不变。旧选区/意见保留旧基准不自动迁移，用户知道新页或新版待处理。
- **W09-AC05**：仅正文项目可直接编辑标题/正文，保存后刷新及切页内容准确，草稿按项目/页/基准隔离，错误/未知保存保留输入并复用原operation核实。改P12事实只使实际受影响页/层待更新；原图是否需重做由显式计划决定，不能把保留旧图说成已适用。
- **W09-AC06**：材料或受众/用途变化由Host提交影响依据并保留既定决定；无影响须具体unchanged_reason，不按时间/字数判断，不默认整套重制或重复确认已提供事实；输入对齐不依赖界面已查看。
- **W09-AC07**：真实材料替换→影响判断→局部稿更新证明未改页保持；同组用例覆盖改受众/用途、保留既定决定及避免无依据整套重制。失败和输入变更晚到结果不能覆盖新稿，真实Host与合成用例分别留证。

所有编号只有本卡为最终验收 owner。自动化、浏览器、真实 Host 与用户验收分别记录；未执行项保持未验证。

## 示例与验证交付

按 [DX-SPEC](../DX-SPEC.md) §3/§5/§6/§7随实现交付可运行材料替换/内容变更/整稿操作脚本，建议examples/workbench/w09_content_inputs.py（未来文件）。在临时项目由真实inputs/view回包取版本/页/来源ref，复用现有inputs与W06计划交接，不固定假ID、不触碰用户材料。覆盖重排/移除/合并拆分/输入协调及晚到结果，记录CLI/同源浏览器错误恢复与前后ref矩阵；真实Host的影响判断和局部稿更新单列，fixture不能冒充已读取材料。

## 失败与恢复

材料读取失败保留原材料版本与原因；输入保存成功但Host未返回时旧成品可看但不可误交。冲突保留本地稿与服务端稿。移除/恢复都走新版本，不删不可变对象。

## 独立交付与回退

核心输入/ContentPlan增量先main、前端接入后交付完整改内容路径；不扩大到外部素材管理平台。回退停新编辑入口，保留读与已有CLIinputs；恢复用当前新版本事务而非重置调用事实。
