# W01 · 链路读模型

状态：核心读取切片已实施并完成相称的 core/HTTP/旧入口浏览器验证；完整压力与 W02/W07 对象适配待补，整卡未验收。实现提交 b0ac9e5，详见 [W01 执行证据](../../../reports/workbench-v3-execution-20260926/W01.md)。执行基线：d1c7c4600cb0fc781070116ed8cc8a1ff6b8b7ca；产品真相见 [PLAN](../PLAN.md)，基线实现见 [CODE-BASELINE](../CODE-BASELINE.md)。

## 用户结果

打开现有项目便能看到每页已有的逐页稿、请求、原图、SVG、PPT预览以及来路；缺失信息明确显示“未记录”，历史页不串到当前稿。

## 范围与不做

增加统一工作台只读摘要及按页链路详情，读当前或指定 revision。分开存在性/适用性、执行、候选采用、人的关注四类状态；同页产物可不同步。输出必须标 per_page/whole_deck、关系可信度及来源对象。

不做：写入新大纲、推测并补写历史 prompt、扫描用户目录找关联、改变 continue 编排、任何生图。

## 依赖与拆分

依赖当前活动核心；先提交核心投影及契约测试，再提交只读 Web 路由。是 W02–W06 的公共底座。

## 契约与服务复用

工程约束见 [ENGINEERING-SPEC](../ENGINEERING-SPEC.md) §1/§3/§6/§7：按revision及真实依赖缓存摘要，候选/Attempt详情按需读取；generation_basis、adoption_target和project CAS分别投影，不把无关页完成说成候选过期。300页×5候选×3Attempt压力下warm /api/view/summary p95≤250ms；保留旧Host头、对象白名单/hash、SVG与文本安全防线，新增GET不要求读取token。以上均需实现验证。

扩展现有 deck_view.v1 保持既有字段；可另设 workbench 版本投影，但只能由同一 Document/Store 派生。提议 GET /api/workbench?revision= 和 /api/pages/{id}/lineage?revision=；返回 project/revision、page顺序、阶段产物引用、task/attempt关联、known/derived/unknown、适用性依据。复用 Store.load_document、task_summary 和质量摘要，扩展 project_view 接收 revision；旧任务 result_refs 不完整时如实返回。

## 源码入口

`src/deck_master/view.py:20`、`web.py:165`、`service.py:394`、`models.py:435`、`store.py:193`；活动 `resources/contracts/{document,task,artifact,page}.…schema.json`。测试入口 `tests/rebuild/test_web.py`、`test_service_flow.py`。

## 行为验收

- **W01-AC01**：24页混合阶段摘要保留顺序/page_id及未生成/当前/依据变化/历史/未知，不拿原图冒充PPT预览；按ENGINEERING-SPEC§6在300页×5候选×3Attempt压力下warm loopback summary p95≤250ms，详情按需读取，不在摘要展开全部候选/Attempt。
- **W01-AC02**：读取历史 revision 后背景出现新结果，历史页、prompt引用及PPT所属快照保持不变；读取不产生新revision。
- **W01-AC03**：无 submitted_prompt 的旧图显示未记录；prepared 与 submitted 可分别读取，不能用重算文本填充 submitted。
- **W01-AC04**：整稿PPT明确依赖有序页面集，页预览明确所属整稿/检查快照；确证不足时标推导或未知。
- **W01-AC05**：单个对象损坏不让整稿白屏；该层返回局部错误和恢复入口，已知的其他页仍可读。
- **W01-AC06**：旧/api/view字段兼容；新GET保留loopback Host检查、注册项目/路径与hash白名单，拒绝不存在/跨项目revision或对象且不泄露路径。只读不新增token/cookie要求；保留文本数据化与SVG隔离，新旧安全负例均通过。

所有编号只有本卡为最终验收 owner。自动化、浏览器、真实 Host 与用户验收分别记录；未执行项保持未验证。

## 示例与验证交付

按 [DX-SPEC](../DX-SPEC.md) §3/§6/§7随实现交付可运行只读投影脚本、有效临时项目与新旧记录fixtures；建议examples/workbench/w01_lineage.py（未来文件，本轮不创建）。先用当前核心创建临时项目，从view/history真实回包取得project/revision/page/ref再查询固定版本链路，不依赖后续W03工厂或W06写接口。断言历史阅读不写入、缺记录诚实降级、跨页来源不串及分页资源上限；CLI/HTTP读取共享Service并保留既有字段/错误契约。新增--revision/--lineage等能力未实现前标提案，不能用假hash展示成功。

工程验证脚本须覆盖 [ENGINEERING-SPEC](../ENGINEERING-SPEC.md) 本卡对应场景，保留原始输入/响应、故障点或性能采样；核心切片已有上述执行证据，完整 300×5×3 压力条件保持待补。

## 失败与恢复

对象缺失显示具体层级与可用历史；不擅自修复生产项目。summary探测失败保留最后成功快照并标断线时间。版本不存在返回可恢复错误，不回落当前伪装成功。

## 独立交付与回退

独立 PR 提交核心与只读接口、合成JSON示例、兼容测试结果；不默认替换旧UI。回退关闭新路由入口即可，不改持久化格式。

## 本线程修订的首个执行切片

先复用已支持 revision 的 project_view，加安全 snapshot 读取：ID/path/symlink/project/已提交 parent 可达性，扩到 view/page/lineage/task/review 所有读路由。损坏 review/task 不能绕过局部错误处理导致整稿崩溃或显示质量 pass。cli.py 加明确 --revision/--summary/--page-id --lineage，保留默认服务状态和 --open，读取选项与 --open 互斥。

首轮可交付 core/http/browser 安全证据；300 页基础采样只作阶段证据。W01-AC01 全部条件仍待 W02/W07 的真实契约压力工厂，W07 后回访本卡，不降低 300×5×3 或 p95≤250ms。状态分核心切片与最终 AC，不声称整卡关闭。
