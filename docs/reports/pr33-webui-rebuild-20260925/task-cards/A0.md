# A0 flow-quality 批次交付（T1–T8）与验收

| 项 | 内容 |
| --- | --- |
| 阶段 / 类型 | A 路 / 交付 + 验收（不新增功能） |
| 依赖 | 无，**现在可做** |
| 执行者 | 现有 `codex/flow-quality` 会话（工作目录 `Deck-Master/`，其他 Agent 不得接管该工作树） |
| 验收人 | Claude Code |
| 分支 / PR 目标 | `codex/flow-quality` → **`main`** |
| 状态 | 进行中（2026-09-26 核对：T1–T7 已本地提交至 `140ff9f`，未推送；T8 在工作区未提交） |

## 目标

把 flow-quality v1.1 已完成的 T1–T8 推送、开 PR 并通过验收，让 `main` 获得：任务事实与方法派发、CLI 任务字段、目录材料入口、`inputs show/update`、`content_update` 接收与出口闸门、按缺口派发的最终审阅、单一 Skill/方法源、旧 Skill 删除。这是 T9、S-N04 和 B 路 P2 之后所有卡的基线。

## 背景与输入

- 规格包（只读，合成数据）：`~/Downloads/Deck_Master_Flow_Quality_Spec_v1.1_20260925/`，先 `cd` 进去跑 `shasum -a 256 -c FILES.sha256`。SPEC.md / TASKS.md / ACCEPTANCE.md 是权威。
- 已有提交：`375af7c` T1 · `800f754` T2 · `1e91c2c` T3 · `11d015e` T7 · `8a8e051` T4 · `c87ff8b` T5 · `140ff9f` T6。
- 已知验证：`c87ff8b` 在独立 worktree 自建 `.venv` 下 470 passed（Claude 2026-09-26 复跑）。
- **会影响 B 路的共享改动**：T5 改了 `view.py`（`input_alignment`、`reconciliation{notice, reason}`）和 `resources/static/app.js`、`style.css`（待更新时隐藏编辑控件）。PR 描述中必须单列。

## 可改范围 / 禁止

- **可改**：只收尾 T8（SPEC §7.2），以及修复验收中退回的问题。
- **禁止**：在本卡中做 T9；动 `~/.codex`、`~/.deck-master` 的真实内容；发版、打 tag、自行合并。

## 实施步骤

- [ ] 1. 完成 T8：`git rm` 14 个 `skills/deck-*`（除 deck-master）、4 个 `skills/ppt-*`、`skills/manifest.json`、`skills/stage-contracts.json`、`skills/deck-master/prompts/`、`skills/deck-master/schemas/`；`RESOLVER.md` 改成一行指向 `skills/deck-master/SKILL.md`；同步 AGENTS.md、CLI 帮助、在用文档；保留 cli.py 的 legacy run 检测。
- [ ] 2. 为 T8 补测试：`skills/` 只剩 deck-master 和 RESOLVER.md；除历史文档外 `rg` 不到旧 Skill 名；legacy run 提示测试仍然通过。
- [ ] 3. `git fetch && git rebase origin/main`，跑完整测试，全绿。
- [ ] 4. `git push -u origin codex/flow-quality`，开 PR 到 `main`。PR 描述逐条列出 AC-01…AC-16 对应的测试名 / 命令输出（证据类型 A 或 R），并单列对 `view.py` / `static/` 的改动。
- [ ] 5. 回填交接，通知验收人。

## 验收标准

| AC | 标准 | 对应 |
| --- | --- | --- |
| AC1 | 完整测试全绿（验收人亲自复跑） | 全部 |
| AC2 | ACCEPTANCE.md 的 AC-01…AC-16 每条都有测试或真实命令输出，并附测试名 | 规格 |
| AC3 | 规格要求的负向回归全部有测试：旧 scoped 结果在任务事实变化后不能被接收；材料目录里的坏文件不能变成假成功；未改页不重新生成；旧 must_fix 不能被普通 pass 覆盖；待更新期间不能 delivery；读回失败不能 delivery；方法文件不从源码 CWD 读取 | ACCEPTANCE |
| AC4 | c93 旧项目夹具可以加载，并显示为 `legacy_current` | 兼容 |
| AC5 | diff 没有越出 T1–T8 的范围，也没有残留调试代码或 TODO | 自检 |

## 验收方法

1. 在独立 worktree（`git worktree add /tmp/a0-accept origin/codex/flow-quality`）**自建 `.venv`**，然后跑完整测试。不要借用其他 worktree 的 venv，因为 editable 安装绑定路径，借用会导致 4 条 install 测试失败。
2. 抽查 AC-05、AC-07、AC-10、AC-13、AC-14、AC-16，用真实 CLI 在 `/tmp` 下的示例项目上复现。
3. 检查 `view.py` / `static/` 的改动与 B 路现有前端测试没有冲突。
4. 结论写入下表。**通过后由老板授权合并**。合并后通知 B 路：`codex/webui-rebuild` 需要 rebase。

## 交接回填

```text
执行者 / 最终 SHA / PR 链接 / 完成日期：
测试命令与结果：
AC-01…AC-16 证据清单：
对 view.py / static 的改动摘要：
未验证项（AC-17 属 T9⑥，不在本卡）：
```

## 验收记录

| 日期 | 验收人 | 结论 | 说明 |
| --- | --- | --- | --- |
