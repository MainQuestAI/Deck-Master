# PR #39 修复验证

最新：[第二轮六项修复与最终回归](round2.md)。以下保留首轮验证记录。

日期：2026-09-26。范围：A 路 Flow Quality 首轮 review 的 5 项问题。

## 行为与回归

- 正文、页面集合或顺序变化时，在采纳事务内退休当前整稿输出；无变化保留。未改页槽位与历史产物保留。
- 恢复历史内容时携带历史依据，旧格式按历史 task/sources 补齐依据；空稿移除依据。当前任务要求、停止策略和调用事实保持现有恢复语义。
- 显式 continue 在项目锁内恢复 input_revision，复用有效任务并替代不兼容制作任务；取消的旧结果仍拒绝。恢复来源与用户变更原因保持可追溯。
- 提示条放到页面网格外；旧 Skill 名扫描使用标准库，测试中清空 PATH，实际执行检查。

先执行新增状态流回归，修复前 **8 failed / 2 passed**；修复后这些回归全部通过。最终本地 Python 3.12：

```text
python -m pytest tests/rebuild -q
528 passed in 106.28s
python -m ruff check src/deck_master tests/rebuild
All checks passed!
git diff --check
(exit 0)
```

真实渲染回归覆盖重排、删页、增页和正文变化，经 continue 的逐页审阅/编译流程生成新 PPT，并直接读取 PPTX slide XML 验证顺序与文字。旧文件在重建前后均不能通过 delivery handoff。工作流中的 Host 审阅使用合成测试记录，不构成专业内容验收。

GitHub Python 3.11/3.12 unit 与真实渲染组的最终结果，以 PR #39 当前提交的 Checks 为准。

## Chromium 浏览器证据

本地真实工作台、合成三页项目，连续执行 pending → current → 第二次 pending。长变更原因、提示移除、重复更新均检查。

| 视口 | 页面列表宽度 | 正文 x / 宽度 | 控件 |
|---|---:|---:|---|
| 1280×900 | 220 | 220 / 1060 | 保存、反馈可用 |
| 1440×900 | 220 | 220 / 1220 | 保存、反馈可用 |

第二次更新只有一个提示条，显示第二次业务原因，正文仍为 1220px。第一次修订采纳后不刷新整个浏览器，仅点击工作台刷新按钮，提示消失且编辑控件可用。

- [待更新 1280](pending-1280.png)、[待更新 1440](pending-1440.png)
- [当前 1280](current-1280.png)、[当前 1440](current-1440.png)
- [第二次更新 1440](second-update-1440.png)

## 验证边界

本轮不执行真实 HOME 安装迁移或 AC-17 真人会话。早先 dc2636e 安装候选不包含本轮状态流修复，不能作为本轮修复后的安装验收对象；后续安装验收需要从最终提交重新构建候选。
