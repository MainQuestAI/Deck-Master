# Deck Master Skill Suite 1.1.0 — Release Notes

日期：2026-06-24 · 基线：`4605213` · 目标版本：`1.1.0`

## 主题

从「Skill 命名体系 + 命令集合」升级为 **Skill OS Runtime**：每个生产阶段
机器可读地回答——谁负责、能否进入、缺什么、是否完成、下一步是谁、是否需确认、
确认绑定了哪些产物、上游变化后哪些结果已过期。

## Stack A — Stage Contract & Handoff Runtime

- A0 基线冻结（main `4605213`，836 passed）
- A1 Canonical Manifest（1.1.0 + stage_id）+ 9 Stage Contracts + loader/validator + contract hash
- A2 Workflow State Resolver（9-stage 派生、entry/exit 校验、fingerprint/stale 传播、可重建快照）
- A3 Handoff Runtime（append-only、idempotent、file lock、supersede、current 投影）
- A4 Approval & Preauthorization（绑定 handoff + fingerprint、non-bypassable、final export 不可预授权）
- A5 Workflow CLI（`workflow status/stages/handoff/approval/preauth/autopilot`）+ manifest 驱动 route + 四入口一致

## Stack B — Production Boundary & Autopilot v2

- B1 Forcing Questions + Decision Log（gap-only、blocking 阻断 exit、answer 绑 input_fingerprint、stale 重现）
- B2 Sourcing Plan v2（per-page 6 类决策、authority/freshness/permission、coverage、v1 安全迁移）
- B3 Page Package v1（customer_visible/internal_only 严格分区、leak 检测、claim/evidence/asset 绑定）
- B4 Build Manifest v2（白名单投影、package/customer_payload hash、backend 契约版本校验、preview 直接输入阻断、显式 legacy adapter）
- B5 Autopilot v2（5 模式、validate→action→exit→handoff、approval 停、final export 永停、repair routing、evidence）

## Stack C — Review Desk, Compat & Release

- C2 Skill Doc Contract（18 SKILL.md 契约附录、100% public 合规、自动校验器）
- C3 Legacy Migration（bootstrap 不伪造审批、回滚、inference report）
- C4 Release Contract（9 schema 真校验、smoke 流水线、CI 增强、capability lock 含 contracts hash）

## 硬决策落地

D1–D15 全部落地（见 `docs/specs/skill-os-runtime-v1.1/00-master-spec.md`）。
非绕行规则：final client export 永远显式确认；handoff 全 transition 覆盖；
Builder 不读 internal_only。

## 已登记偏差

- DEV-001：本机活动安装早于基线（C5 重新构建 1.1.0 后复核）
- DEV-002：input_type 共享允许（route 按 stage 消歧）
- DEV-003：跨仓库 PPT Deck Pro Max bridge 待独立 PR + 固定 SHA
- DEV-004：state.py 接 handoff runtime（B5 集成所需，向后兼容）

## 测试

全量 `pytest -q`：**1005 passed**。CI 已增加 Skill OS contract/schema 校验与 smoke 步骤。

## 已知未完成

- C1 Review Desk Skill OS 视图（UI 投影）与 C5 真实 dogfood 验收待后续推进（见 deviation log）。
- 跨仓库 bridge（DEV-003）待外部 PR。
