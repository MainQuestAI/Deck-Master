# Deck Master v0.9.14-preview.4 Release Notes

日期：2026-08-10
状态：Technical Preview / agent-operable
公开版本：`v0.9.14-preview.4`
Python 包版本：`0.9.14a4`
Skill Suite 合同版本：`1.1.0`

## 发布定位

这是 `v0.9.14-preview.3` 的安装与能力发现补丁版。High-Density Builder 的
MBB、客户可见内容净化、原生 SVG、DrawingML 和图标视觉验收能力保持不变。

## 本版修复

- 安装后的 release tree 能从 canonical `contracts/` 目录发现全部高密度 v2 合同。
- release 激活时修复虚拟环境中 `pip`、`deck-master` 等 Python 入口的 staging 路径，
  确保命令指向当前安装版本。
- 增加源码路径与安装路径的能力发现回归测试。
- 增加虚拟环境搬迁后的 Python shebang 回归测试，并保护 shell 激活脚本不被误改。

## 验证结果

- 发布运行时与 High-Density Builder 回归：`110 passed`，`4 subtests passed`。
- Ruff、Python compile 检查和 `git diff --check` 通过。
- 本机 release tree 重新安装并完成 release smoke。
- 安装态 High-Density capability 的合同检查、渲染器、Python 包和字体检查均通过。

## 版本边界

本版本仍是 Technical Preview，不声明正式生产就绪。fresh provider smoke、六页
外部图标验收、73 页外部 benchmark 以及 PPT Library / RC 完整闭环仍保留为后续发布门。

## 安装与核验

```bash
python3.12 -m pip install -e ".[dev]"
python3 scripts/deck_master.py suite-install --target codex --include-optional
deck-master agent-doctor --mode preview --output json
deck-master suite-status --capability deck_master.build.high_density.v1 --output json
```
