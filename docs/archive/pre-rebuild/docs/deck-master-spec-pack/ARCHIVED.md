# ARCHIVED — historical reference only

This spec pack documents the pre-rebuild (v0.9.x) design. It is kept for
history and migration reference; it is NOT the current implementation truth.

Current baseline: `docs/specs/deck-master-rebuild-v1/` (spec pack v1.1).
Entry point and command mapping: `docs/migration-to-rebuilt-core.md`.
Do not implement new work against this pack.
