"""Native PPTX compile kernel (SC-1.1 ND-01).

Single implementation of the SVG→PPTX compile, parse, paint, canvas and
visual-check pipeline, extracted from the high-density builder. Both the
native default route and the legacy high-density entry load this package;
there is exactly one implementation.
"""
