"""Deck Master rebuilt core (rebuild v1.1 spec pack).

``__init__`` exposes the version only. It never imports the engine, scans
HOME, or loads fonts (spec 01.2).
"""

from __future__ import annotations

__version__ = "1.0.0.dev2"
