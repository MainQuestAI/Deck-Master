{
  "schema_version": "deck_master_capability_package.v1",
  "name": "ppt-deck-pro-max",
  "kind": "product_capability",
  "required": true,
  "runtime": {
    "operations": ["status", "doctor", "schema", "smoke --fixture", "writeback"],
    "default_command": "deck-master run-generation"
  },
  "contracts": {
    "outputs": ["ppt_deck_pro_max_generation_result.v1", "deck_generation_result.v1"]
  },
  "state_policy": {
    "active_run_outputs_must_import": true,
    "requires_session_binding": true
  }
}
